# LMS — Fix & Outstanding Backlog (compiled 2026-05-24)

Consolidated from the full UAT (all roles). Tick `[ ]` → `[x]` as we work through them. Effort: **S** ≈ minutes, **M** ≈ an hour or two, **L** ≈ a real build. UAT found **zero functional defects** — everything below is cosmetic, an enhancement, config, an unbuilt feature, or a still-to-verify item.

---

## A. Fixes (defects found in UAT)
- [ ] **D-1** · S · *cosmetic* — Admin **department detail** page (`/admin/departments/:id`) top bar reads "Dashboard". Fix the `AdminLayout` page-title resolution so detail routes show "Departments". → `frontend/src/components/layout/AdminLayout.tsx`.
- [ ] **D-2** · S · *cosmetic* — "My Required Training" / Departments label a course **that has a published podcast** as "Podcast" (because `is_podcast = course.ilb_published`). Decide intended label: keep, or reserve "Podcast" for standalone broadcasts and show course-attached ones as "Course". → `backend/services/department_service.py` (is_podcast) + `frontend .../MyTraining.tsx` / `DepartmentDetailPage.tsx`.

## B. Enhancements (UX)
- [ ] **E-1** · M — **Podcast/ILB player autoplay** (the "not flowing" issue). Auto-advance to the next segment on **audio-end**; **timer fallback** (scaled to word count) when there's no narration audio; honour **Play/Pause**; pause on a Q&A then resume (interrupt) / keep flowing (defer); stop at the last segment + nudge to Finish; keep Prev/Next for manual control. → `frontend/src/pages/learn/ILBPlayerPage.tsx`. *(Spoken autoplay also needs C-1; timer flow works without it.)*

## C. Config — prod env vars (set on the Coolify **backend** service)
- [ ] **C-1** — `ELEVENLABS_API_KEY` → podcast **narration audio** (TTS). Currently returns clean 503; podcast plays text-only.
- [ ] **C-2** — `DEEPGRAM_API_KEY` → podcast **voice questions** (STT). Currently 503; voice input disabled.
- [ ] **C-3** · optional · L — **HeyGen** avatar integration/key → live avatar video (currently fully stubbed).

## D. Feature builds (product gaps — not bugs, not yet implemented)
- [ ] **P-1** · L — Learner **quiz-taking** (take quiz, attempts, pass/fail, feedback timing). Authoring exists; no learner play/submit endpoint found.
- [ ] **P-2** · M/L — **Certificates** on course completion (`certificate_enabled` flag exists; no generation/download).
- [ ] **P-3** · L — Learner **course player** for slide-based courses (currently a placeholder page; the relational slide/block player rebuild).

## E. UAT — still to verify (couldn't auto-test; low risk)
- [ ] **T-1** — Security **mutations**: IP-allowlist add/remove (⚠️ can lock out logins — test carefully) and **kill-session** (force-logout). `/admin/security`.
- [ ] **T-2** — User **CSV bulk-import** (upload a CSV on `/admin/users`).
- [ ] **T-3** — White-label **logo/favicon upload** + theme **export/import** (`/admin/whitelabel`).
- [ ] **T-4** — Course **progress → completion** via the learner player (gated by P-3).
- [ ] **T-5** — Creator **slide-editor** + **quiz-builder** UI walk-through (block canvas, drag/resize, autosave, undo/redo) — APIs already pass.
- [ ] **T-6** — Podcast **authoring UI** walk-through (script → voice → render → publish) — APIs already pass.

## F. Tech debt / deferred
- [ ] **TD-1** · S — `backend/requirements.txt` pins `pillow==10.1.0` (no Python-3.14 wheel; builds fine on Coolify's older Python but a fresh 3.14 install fails). Bump to `12.2.0` (the proven-working version) when convenient.
- [ ] **TD-2** · M — **Email reminder *sending*** (the deferred D6 feature): pick a transport (SMTP / SendGrid / SES) + creds + a Coolify cron, then build a "send due reminders" endpoint off the existing `/api/departments/reminders` data.
- [ ] **TD-3** · S/M — Batch the compliance + `/reminders` queries (currently O(members × content) per request) before the email job scales. (Already flagged as a follow-up.)

## Notes — working-as-designed (no action)
- Editing a published course flips it to `has_unpublished_changes` and hides it from the learner catalogue until **re-published** (enrolled learners stay version-pinned). Surprising but intentional.
- Test data created during UAT on prod (users `learner1`/`creator1`/`temp1`(deactivated), "UAT Dept", "UAT Test Course", "UAT Broadcast", AI course "SRSUK Absence Management…") — clean up if desired.

---
*Full UAT evidence: `docs/superpowers/UAT-2026-05-24-new-features.md`.*
