---
phase: 18-preview-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_publish_phase18.py
  - frontend/src/pages/creator/__tests__/PreviewMode.test.tsx
autonomous: true
requirements:
  - PREVIEW-01
  - PREVIEW-02
  - PREVIEW-03
  - PUBLISH-01
  - PUBLISH-02
  - PUBLISH-03
  - PUBLISH-04
  - PUBLISH-05
  - PUBLISH-06
  - PUBLISH-07
  - PUBLISH-08

must_haves:
  truths:
    - "Backend test stubs exist and fail with pytest.fail() — not ImportError"
    - "Frontend test stubs fail at import collection with 'Cannot find module'"
    - "No implementation code exists yet — stubs only"
  artifacts:
    - path: "backend/tests/test_publish_phase18.py"
      provides: "RED stubs for all 9 backend test cases"
      contains: "pytest.fail"
    - path: "frontend/src/pages/creator/__tests__/PreviewMode.test.tsx"
      provides: "RED stubs for PREVIEW-01, PREVIEW-02, PREVIEW-03"
      contains: "CoursePreviewPage"
  key_links:
    - from: "backend/tests/test_publish_phase18.py"
      to: "backend/models/models.py"
      via: "No import of routers.courses at top level — avoids ImportError before publish endpoint exists"
      pattern: "pytest.fail"
---

<objective>
Wave 0: Create failing test stubs for all Phase 18 requirements before any implementation exists.

Purpose: Establish the RED state (TDD) for all 11 requirements. Backend stubs use pytest.fail() so tests FAIL cleanly (not ERROR). Frontend stubs import non-existent CoursePreviewPage so vitest fails at collection.

Output: Two test files that fail immediately — GREEN only after Plans 02, 03, 04 implement the features.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-preview-publish/18-RESEARCH.md
@.planning/phases/18-preview-publish/18-VALIDATION.md
@backend/tests/conftest.py
@backend/tests/test_tts_phase17.py
@frontend/src/pages/creator/__tests__/CourseBuilderPage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend test stubs for PREVIEW-01 through PUBLISH-08</name>
  <files>backend/tests/test_publish_phase18.py</files>
  <action>
Create `backend/tests/test_publish_phase18.py` with 9 stub test functions, each calling `pytest.fail("not implemented")`. Do NOT import from `routers.courses` at the top level — publish/archive/preflight endpoints do not exist yet, and a module-level import would produce ERROR (not FAILED), breaking the RED state.

Use the same fixture pattern as test_tts_phase17.py: import from conftest (setup_test_db, db, creator_user, creator_token), file-local `creator_course_publish` fixture if needed.

File structure:
```python
import pytest
from fastapi.testclient import TestClient
from main import app

client = TestClient(app)

# No top-level import of non-existent endpoints

def test_preview_endpoint_returns_draft_course(db, creator_token, creator_course):
    pytest.fail("not implemented — PREVIEW-01")

def test_preview_includes_slides_and_blocks(db, creator_token, creator_course):
    pytest.fail("not implemented — PREVIEW-02")

def test_preflight_returns_results(db, creator_token, creator_course):
    pytest.fail("not implemented — PUBLISH-02")

def test_preflight_fails_no_modules(db, creator_token, creator_course):
    pytest.fail("not implemented — PUBLISH-03")

def test_preflight_fix_urls_present(db, creator_token, creator_course):
    pytest.fail("not implemented — PUBLISH-04")

def test_publish_transitions_to_published(db, creator_token, creator_course):
    pytest.fail("not implemented — PUBLISH-05")

def test_publish_creates_version_snapshot(db, creator_token, creator_course):
    pytest.fail("not implemented — PUBLISH-06")

def test_learner_gets_enrolled_version(db, creator_token, creator_course):
    pytest.fail("not implemented — PUBLISH-07")

def test_archive_hides_from_catalogue(db, creator_token, creator_course):
    pytest.fail("not implemented — PUBLISH-08")
```

conftest.py already has `creator_course` fixture (line 214) — reuse it directly, no file-local duplicate needed.
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend" && source venv/bin/activate && python -m pytest tests/test_publish_phase18.py -x -q 2>&1 | grep -E "FAILED|passed|error" | head -5</automated>
  </verify>
  <done>All 9 tests show FAILED (not ERROR). Zero passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend test stubs for PREVIEW-01, PREVIEW-02, PREVIEW-03</name>
  <files>frontend/src/pages/creator/__tests__/PreviewMode.test.tsx</files>
  <action>
Create `frontend/src/pages/creator/__tests__/PreviewMode.test.tsx` that imports the non-existent `CoursePreviewPage` component — vitest will fail at collection with "Cannot find module", establishing the RED state.

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MemoryRouter, Route, Routes } from 'react-router-dom'
// This import does not exist yet — vitest fails at collection (RED state)
import { CoursePreviewPage } from '../CoursePreviewPage'

vi.mock('@/services/api', () => ({
  api: {
    get: vi.fn().mockResolvedValue({ data: { id: 1, title: 'Test Course', modules: [] } }),
  },
}))

vi.mock('@/context/AuthContext', () => ({
  useAuth: () => ({ user: { id: 1, role: 'creator' }, isAuthenticated: true }),
}))

describe('CoursePreviewPage', () => {
  it('renders draft watermark (PREVIEW-01)', () => {
    // stub — fails at collection before implementation
    expect(true).toBe(false)
  })

  it('shows exit preview button that navigates to returnTo (PREVIEW-03)', () => {
    // stub — fails at collection before implementation
    expect(true).toBe(false)
  })

  it('displays course modules and content (PREVIEW-02)', () => {
    // stub — fails at collection before implementation
    expect(true).toBe(false)
  })
})
```

Do NOT create `CoursePreviewPage.tsx` — the import must fail at collection.
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/frontend" && npm run test:unit -- --run PreviewMode 2>&1 | grep -E "Cannot find|FAIL|error" | head -5</automated>
  </verify>
  <done>vitest fails at collection with "Cannot find module '../CoursePreviewPage'". No test passes.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_publish_phase18.py -q` → 9 FAILED, 0 passed, 0 errors
- `cd frontend && npm run test:unit -- --run PreviewMode` → collection error on missing CoursePreviewPage import
</verification>

<success_criteria>
Wave 0 complete: all test stubs exist, all fail cleanly, no implementation code created. Plans 02-04 can now target GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/18-preview-publish/18-01-SUMMARY.md`
</output>
