---
phase: 18-preview-publish
plan: 05
type: execute
wave: 5
depends_on:
  - 18-03
  - 18-04
files_modified: []
autonomous: false

requirements:
  - PREVIEW-01
  - PREVIEW-02
  - PREVIEW-03
  - PUBLISH-01
  - PUBLISH-02
  - PUBLISH-03
  - PUBLISH-04
  - PUBLISH-05
  - PUBLISH-06
  - PUBLISH-07
  - PUBLISH-08

must_haves:
  truths:
    - "Creator can enter preview mode and see all course content with draft watermark"
    - "Creator can exit preview and land back in Course Builder"
    - "Creator can open preflight, see colour-coded results, and deep-link to fix failures"
    - "Creator can publish a draft course — it appears in learner catalogue"
    - "Creator can archive a published course — it disappears from learner catalogue"
  artifacts:
    - path: "frontend/src/pages/creator/CoursePreviewPage.tsx"
      provides: "Fully verified preview page"
      contains: "preview-watermark"
    - path: "frontend/src/components/publish/PreflightModal.tsx"
      provides: "Fully verified preflight modal"
      contains: "preflight-result"
  key_links:
    - from: "Course Builder"
      to: "Preview Mode"
      via: "Preview button → /creator/courses/:id/preview?returnTo=..."
      pattern: "PREVIEW-01"
    - from: "Course Builder"
      to: "Preflight Modal → Publish"
      via: "Publish button → PreflightModal → confirm-publish-btn"
      pattern: "PUBLISH-05"
---

<objective>
Human verification: Full walkthrough of preview mode, pre-flight checklist, publish flow, archive, and version pinning in the browser.

Purpose: Confirm all 11 requirements (PREVIEW-01 through PUBLISH-08) work end-to-end with live data. Gate before marking Phase 18 complete.

Output: Creator confirms each numbered check below. Any failures are fed back for gap closure.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-preview-publish/18-RESEARCH.md
@.planning/phases/18-preview-publish/18-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify — 10 browser checks (PREVIEW-01 through PUBLISH-08)</name>
  <action>Human verifies all 10 checks described below in the browser with live dev servers running.</action>
  <what-built>
    Full Phase 18 implementation:
    - CoursePreviewPage with amber watermark bar and returnTo exit
    - GET /api/courses/:id/preview endpoint (bypasses PUBLISHED filter)
    - GET /api/courses/:id/preflight endpoint with pass/warn/fail rules
    - POST /api/courses/:id/publish with CourseVersion snapshot + status transition
    - POST /api/courses/:id/archive with status transition
    - Enrolled learner version pinning (learn.py snapshot fallback)
    - PreflightModal and PublishConfirmModal in Course Builder
    - Alembic migration 005_course_versions.py
  </what-built>
  <how-to-verify>
Start the dev servers (frontend + backend) and verify each check in order:

**Setup:** Create or use a DRAFT course with at least one module, one video with slides, and one quiz with 3+ questions.

---

CHECK 1 — Preview Mode entry (PREVIEW-01):
1. Open Course Builder for the draft course
2. Click the "Publish" button — it should open the PreflightModal
3. Close the modal, then find the "Preview" button in the header
4. Click Preview → you should be navigated to /creator/courses/:id/preview
5. Confirm: amber watermark bar appears at the top reading "Preview Mode — Draft"

CHECK 2 — Preview content (PREVIEW-02):
6. While in Preview Mode, scroll through the course content
7. Confirm: modules are listed, video titles visible, quiz titles visible
8. Confirm: narration script text visible in slide detail (if populated)

CHECK 3 — Exit Preview (PREVIEW-03):
9. Click "Exit Preview" button in the amber bar
10. Confirm: you land back on the Course Builder page (not a blank page, not login)

CHECK 4 — Pre-flight checklist (PUBLISH-02, PUBLISH-03):
11. In Course Builder, click the "Publish" button
12. Confirm: PreflightModal opens with a list of check results
13. Confirm: each result shows a Pass / Warn / Fail label in the correct colour (green/amber/red)
14. Confirm: thumbnail warning shows if no thumbnail is uploaded (WARN, not FAIL)

CHECK 5 — Pre-flight deep-links (PUBLISH-04):
15. If any FAIL item appears, click its "Fix" link
16. Confirm: you are navigated to the exact screen needed to fix the issue
17. Navigate back to Course Builder and re-open the PreflightModal

CHECK 6 — Publish (PUBLISH-05):
18. Ensure all FAIL checks pass (add a module with a quiz with ≥3 questions if needed)
19. Click "Publish" in the PreflightModal
20. Confirm: PublishConfirmModal opens
21. Click "Confirm & Publish"
22. Confirm: modal closes, course status updates (button or status indicator changes)

CHECK 7 — Course visible to learner (PUBLISH-05 continued):
23. Log in (or open incognito) as a learner account
24. Navigate to the learner catalogue (/learn)
25. Confirm: the published course appears in the catalogue

CHECK 8 — Version snapshot (PUBLISH-06):
26. Back as creator, make an edit to the course (change a module title)
27. Re-publish the course
28. Confirm: publish succeeds (no error)

CHECK 9 — Enrolled learner version pinning (PUBLISH-07):
29. If possible, confirm enrolled learner still sees the original version title after step 28
30. (If only one user available: verify via backend that CourseVersion rows exist with correct snapshots — `SELECT * FROM course_versions;`)

CHECK 10 — Archive (PUBLISH-08):
31. Back as creator, click "Archive" button in Course Builder
32. Confirm: course status changes to archived
33. Log in as learner, check catalogue
34. Confirm: archived course no longer appears in the catalogue

---

Report: "approved" if all 10 checks pass, or describe any failures with which check number failed.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe failures (e.g. "Check 5 failed — Fix link navigates to wrong URL")</resume-signal>
</task>

</tasks>

<verification>
Human verification checkpoint — all 10 checks must pass before Phase 18 is complete.
</verification>

<success_criteria>
All 10 browser checks approved. Phase 18 complete: PREVIEW-01 through PUBLISH-08 all verified end-to-end. Milestone v1.0 AI Course Builder is DONE.
</success_criteria>

<output>
After completion, create `.planning/phases/18-preview-publish/18-05-SUMMARY.md`

Update STATE.md:
- Set Phase 18 status to COMPLETE
- Note milestone v1.0 AI Course Builder complete
- Record any decisions from the verification session
</output>
