---
phase: 16-quiz-builder
plan: 05
type: execute
wave: 4
depends_on: [16-04]
files_modified: []
autonomous: false
requirements: [QUIZ-01, QUIZ-02, QUIZ-03, QUIZ-04, QUIZ-05, QUIZ-06, QUIZ-07, QUIZ-08]

must_haves:
  truths:
    - "Creator navigates from Course Builder tree to Quiz Builder page"
    - "Creator creates a quiz and configures pass score, max attempts, and feedback setting"
    - "Creator adds all four question types (MCQ single, MCQ multi, true/false, short answer) with correct answers"
    - "Creator adds explanation text to a question and it persists after reload"
    - "Creator drags questions to reorder; new order persists after page reload"
    - "Creator clicks AI Generate, sees questions stream in SideDrawer, confirms them to the quiz"
  artifacts:
    - path: "frontend/src/pages/creator/QuizBuilderPage.tsx"
      provides: "All 5 success criteria verifiable in browser"
    - path: "backend/routers/quizzes.py"
      provides: "All CRUD + SSE endpoints live and callable"
  key_links:
    - from: "CourseTreeRail quiz row click"
      to: "QuizBuilderPage at /creator/courses/:id/quizzes/:quizId"
      via: "React Router route"
      pattern: "navigate.*quizzes"
---

<objective>
Human verification of the complete Quiz Builder in a live browser — all 4 phase success criteria confirmed working end-to-end.

Purpose: Gate on human eyes. Tests confirm code logic; only a browser confirms the full creator experience works.
Output: All 4 Phase 16 success criteria confirmed approved or issues documented for gap closure.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-quiz-builder/16-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify — 6 browser checks (QUIZ-01 through QUIZ-08)</name>
  <action>Human verifies all 6 checks described below in the browser with live dev servers running.</action>
  <what-built>
Complete Quiz Builder feature:
- QuizBuilderPage at /creator/courses/:id/quizzes/:quizId (settings, question CRUD, drag-reorder, AI generation)
- QuestionForm with all 4 question types (MCQ single, MCQ multi, true/false, short answer) and explanation field
- dnd-kit drag-to-reorder with POST /api/quizzes/{quizId}/questions/reorder
- AI question generation SideDrawer using useSSEStream + bufferRef pattern
- CourseTreeRail quiz items navigate to QuizBuilderPage
- App.tsx route registered
  </what-built>
  <how-to-verify>
Start the dev server if not already running: `cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/frontend" && npm run dev`

Run these checks in order:

**Check 1 — Navigate to Quiz Builder from Course Builder tree (QUIZ-01 navigation):**
1. Go to http://localhost:5174/lms/creator/courses (or the active dev URL)
2. Open an existing course in Course Builder
3. In the left tree rail, find a quiz item under any module
4. Click the quiz item
5. Confirm: navigates to /creator/courses/:id/quizzes/:quizId and QuizBuilderPage renders with quiz title visible

**Check 2 — Configure quiz settings (QUIZ-01):**
1. On QuizBuilderPage, find the Quiz Settings section
2. Change Pass Rate to 70, Attempts Allowed to 2, Show Feedback to "After completion"
3. Click Save Settings
4. Reload the page
5. Confirm: settings show 70, 2, "After completion" after reload (not reset to defaults)

**Check 3 — Add all 4 question types (QUIZ-02 through QUIZ-05):**
1. Click "+ Add Question"
2. Add an MCQ single question: type "What is 2+2?", options "3 / 4 / 5", select 4 as correct, click Save
3. Click "+ Add Question" again, switch type to "Multiple Choice (multi answer)"
4. Add MCQ multi: "Which are even numbers?", options "2 / 3 / 4 / 5", check 2 and 4 as correct, click Save
5. Click "+ Add Question", switch to "True / False"
6. Add T/F: "The sky is blue", select True, click Save
7. Click "+ Add Question", switch to "Short Answer"
8. Add short answer: "Name a programming language", leave correct answer empty, click Save
9. Confirm: 4 questions visible in list with correct type labels

**Check 4 — Explanation text persists (QUIZ-06):**
1. Click Edit on any question
2. Enter "This is the explanation text" in the Explanation field
3. Click Save Question
4. Reload the page
5. Confirm: explanation text visible under the question after reload

**Check 5 — Drag questions to reorder (QUIZ-07):**
1. With 3+ questions in the list, drag the last question to the top position
2. Reload the page
3. Confirm: the dragged question is still at the top after reload (order persisted)

**Check 6 — AI question generation (QUIZ-08):**
1. Click "Generate with AI" button
2. Confirm: SideDrawer opens titled "AI Question Generator"
3. Set count to 3, keep tone as Professional
4. Click "Generate Questions"
5. Confirm: questions stream in and appear in the preview list
6. Click "Add All Questions"
7. Confirm: drawer closes and the generated questions appear in the quiz question list
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass. Describe any issues found if not.</resume-signal>
</task>

</tasks>

<verification>
All 4 Phase 16 success criteria verified in browser:
1. Creator can create a quiz linked to a module and configure pass score, max attempts, and feedback settings
2. Creator can add MCQ single-answer, MCQ multi-answer, true/false, and short-answer questions with correct answers and explanation text
3. Creator can drag questions to reorder them; the new order persists after a page reload with no order_index drift
4. Creator can trigger AI question generation from module content and see a batch of questions streamed in, then confirm them to the quiz
</verification>

<success_criteria>
Human approved all 6 browser checks. Phase 16 success criteria 1-4 confirmed working end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/16-quiz-builder/16-05-SUMMARY.md`
</output>
