---
phase: 13-course-builder-module-detail
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/pages/creator/__tests__/CourseBuilderPage.test.tsx
  - frontend/src/pages/creator/__tests__/ModuleDetailPage.test.tsx
  - backend/tests/test_modules_phase13.py
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
  - BUILD-05
  - BUILD-06

must_haves:
  truths:
    - "@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities are present in frontend/package.json"
    - "CourseBuilderPage.test.tsx exists and fails before implementation (import fails or pytest.fail equivalent)"
    - "ModuleDetailPage.test.tsx exists and fails before implementation"
    - "test_modules_phase13.py exists and fails before implementation"
  artifacts:
    - path: "frontend/src/pages/creator/__tests__/CourseBuilderPage.test.tsx"
      provides: "Failing stubs for BUILD-01, BUILD-02, BUILD-03"
    - path: "frontend/src/pages/creator/__tests__/ModuleDetailPage.test.tsx"
      provides: "Failing stubs for BUILD-04, BUILD-05"
    - path: "backend/tests/test_modules_phase13.py"
      provides: "Failing stub for BUILD-05 SSE endpoint"
  key_links:
    - from: "frontend/package.json"
      to: "@dnd-kit packages"
      via: "npm install"
      pattern: "@dnd-kit/sortable"
---

<objective>
Install dnd-kit and create failing test stubs that establish the RED state for all Phase 13 requirements. All subsequent plans depend on this wave.

Purpose: Nyquist compliance — every task in Waves 1–3 must have automated verify commands. These stubs create the test infrastructure first so tests fail before implementation, then pass after.
Output: dnd-kit installed, 3 test files created in failing state.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-course-builder-module-detail/13-RESEARCH.md
@.planning/phases/13-course-builder-module-detail/13-VALIDATION.md

<interfaces>
<!-- From frontend/src/App.tsx — existing builder stub route to replace later -->
<!-- Line 126: path="/creator/courses/:id/builder" -->
<!-- Wrapped in CreatorLayout + ProtectedRoute creatorRoute -->

<!-- From backend/services/claude_service.py — reusable streaming method -->
async def _stream_text(self, prompt: str) -> AsyncGenerator[str, None]: ...

<!-- From backend/routers/courses.py — SSE pattern (lines 256-289) -->
from sse_starlette.sse import EventSourceResponse
return EventSourceResponse(generator())

<!-- From backend/routers/modules.py — ModuleUpdate schema fields -->
# estimated_duration_minutes (NOT duration_estimate_minutes)
# learning_objectives (NOT learning_outcome) — array
# unlock_rule: str | None
# status: str | None
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dnd-kit and create __tests__ directory</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>
Run from the frontend directory:
```
cd frontend && npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```
This adds all three dnd-kit packages. They are a locked stack decision from STATE.md.

Also create the __tests__ directory if it does not exist:
```
mkdir -p frontend/src/pages/creator/__tests__
```

Verify dnd-kit appears in package.json dependencies under `@dnd-kit/core`, `@dnd-kit/sortable`, and `@dnd-kit/utilities`.
  </action>
  <verify>
    <automated>cd frontend && node -e "const p = require('./package.json'); ['@dnd-kit/core','@dnd-kit/sortable','@dnd-kit/utilities'].forEach(k => { if (!p.dependencies[k]) throw new Error('MISSING: ' + k) }); console.log('dnd-kit OK')"</automated>
  </verify>
  <done>All three @dnd-kit packages present in frontend/package.json dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failing frontend test stubs (RED state)</name>
  <files>
    frontend/src/pages/creator/__tests__/CourseBuilderPage.test.tsx,
    frontend/src/pages/creator/__tests__/ModuleDetailPage.test.tsx
  </files>
  <behavior>
    CourseBuilderPage stubs (BUILD-01, BUILD-02, BUILD-03) — all must FAIL before implementation:
    - Test 1: renders left-rail tree with module titles
    - Test 2: renders status pill (draft/published Badge) for each module
    - Test 3: clicking a module in the tree navigates to /creator/courses/:id/modules/:moduleId
    ModuleDetailPage stubs (BUILD-04, BUILD-05) — all must FAIL before implementation:
    - Test 1: renders form fields for title, description, estimated_duration_minutes, unlock_rule
    - Test 2: submitting the form calls PUT /api/modules/:moduleId
    - Test 3: renders AI generate description button
  </behavior>
  <action>
Create `frontend/src/pages/creator/__tests__/CourseBuilderPage.test.tsx`:

```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MemoryRouter, Route, Routes } from 'react-router-dom'
import { CourseBuilderPage } from '../CourseBuilderPage'

// STUB — fails at import until CourseBuilderPage.tsx is created
describe('CourseBuilderPage', () => {
  const renderPage = () =>
    render(
      <MemoryRouter initialEntries={['/creator/courses/1/builder']}>
        <Routes>
          <Route path="/creator/courses/:id/builder" element={<CourseBuilderPage />} />
          <Route path="/creator/courses/:id/modules/:moduleId" element={<div>Module Detail</div>} />
        </Routes>
      </MemoryRouter>
    )

  it('renders left-rail tree with module titles', () => {
    renderPage()
    // Will fail until implementation — tree rail not yet rendered
    expect(screen.getByTestId('course-tree-rail')).toBeInTheDocument()
  })

  it('renders status pill for each module', () => {
    renderPage()
    // Will fail until implementation
    expect(screen.getByTestId('module-status-pill')).toBeInTheDocument()
  })

  it('renders module overview list', () => {
    renderPage()
    expect(screen.getByTestId('module-overview-list')).toBeInTheDocument()
  })
})
```

Create `frontend/src/pages/creator/__tests__/ModuleDetailPage.test.tsx`:

```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MemoryRouter, Route, Routes } from 'react-router-dom'
import { ModuleDetailPage } from '../ModuleDetailPage'

// STUB — fails at import until ModuleDetailPage.tsx is created
describe('ModuleDetailPage', () => {
  const renderPage = () =>
    render(
      <MemoryRouter initialEntries={['/creator/courses/1/modules/2']}>
        <Routes>
          <Route path="/creator/courses/:id/modules/:moduleId" element={<ModuleDetailPage />} />
        </Routes>
      </MemoryRouter>
    )

  it('renders form fields for module editing', () => {
    renderPage()
    expect(screen.getByTestId('module-title-input')).toBeInTheDocument()
    expect(screen.getByTestId('module-description-textarea')).toBeInTheDocument()
    expect(screen.getByTestId('module-duration-input')).toBeInTheDocument()
    expect(screen.getByTestId('module-unlock-rule-select')).toBeInTheDocument()
  })

  it('renders AI generate description button', () => {
    renderPage()
    expect(screen.getByTestId('ai-generate-description-btn')).toBeInTheDocument()
  })
})
```

Both files import from non-existent source files (`../CourseBuilderPage` and `../ModuleDetailPage`), so vitest will fail at collection — this is the intentional RED state.
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit -- --run CourseBuilderPage 2>&1 | grep -E "FAIL|Cannot find module|failed" | head -5</automated>
  </verify>
  <done>Both test files exist. Running vitest shows collection failures (import errors) for CourseBuilderPage and ModuleDetailPage — confirming RED state established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create failing backend test stub (RED state)</name>
  <files>backend/tests/test_modules_phase13.py</files>
  <behavior>
    BUILD-05 SSE stub — must FAIL before implementation:
    - Test 1: POST /api/modules/:id/ai/generate-description returns 200 with SSE stream
    - Test 2: SSE stream yields data tokens
    - Test 3: Unauthenticated request returns 401/403
  </behavior>
  <action>
Create `backend/tests/test_modules_phase13.py`:

```python
"""
Phase 13 — BUILD-05 tests for POST /api/modules/:id/ai/generate-description SSE endpoint.
STUB: All tests call pytest.fail() — RED state before implementation.
Pattern from Phase 12: test_courses_phase12.py stubs used pytest.fail() directly.
"""
import pytest


def test_generate_module_description_streams_tokens():
    """POST /api/modules/:id/ai/generate-description returns SSE token stream."""
    pytest.fail(
        "BUILD-05 not implemented — POST /api/modules/:id/ai/generate-description "
        "SSE endpoint does not exist yet. Add to backend/routers/modules.py."
    )


def test_generate_module_description_requires_auth():
    """Unauthenticated POST returns 401."""
    pytest.fail(
        "BUILD-05 not implemented — auth guard on SSE endpoint not verified yet."
    )


def test_generate_module_description_404_for_unknown_module():
    """POST for non-existent module_id returns 404."""
    pytest.fail(
        "BUILD-05 not implemented — 404 handling not verified yet."
    )
```

The stubs use `pytest.fail()` directly — same pattern as Phase 12 Wave 0 (from STATE.md decisions-from-12-01). This produces FAILED not ERROR, which is the correct RED state.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_modules_phase13.py -x -q 2>&1 | grep -E "FAILED|passed|failed" | head -5</automated>
  </verify>
  <done>test_modules_phase13.py exists. All 3 tests show as FAILED (not ERROR). RED state confirmed.</done>
</task>

</tasks>

<verification>
After Wave 0 completes:
- `cd frontend && node -e "require('./package.json').dependencies['@dnd-kit/sortable']"` — no error
- `cd frontend && npm run test:unit -- --run CourseBuilderPage` — shows FAIL (import error on CourseBuilderPage)
- `cd frontend && npm run test:unit -- --run ModuleDetailPage` — shows FAIL (import error on ModuleDetailPage)
- `cd backend && python -m pytest tests/test_modules_phase13.py -q` — all 3 FAILED
</verification>

<success_criteria>
- dnd-kit packages (@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities) in frontend/package.json
- CourseBuilderPage.test.tsx fails with import/collection error
- ModuleDetailPage.test.tsx fails with import/collection error
- test_modules_phase13.py has 3 failing tests using pytest.fail()
</success_criteria>

<output>
After completion, create `.planning/phases/13-course-builder-module-detail/13-01-SUMMARY.md`
</output>
