---
phase: 13-course-builder-module-detail
plan: 05
type: execute
wave: 3
depends_on:
  - 13-02
  - 13-03
  - 13-04
files_modified: []
autonomous: false
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
  - BUILD-05
  - BUILD-06

must_haves:
  truths:
    - "Creator sees Course Builder at /creator/courses/:id/builder with left-rail tree and module cards"
    - "Status pills (draft/published) visible on modules in both tree and overview list"
    - "Clicking a module in the tree navigates to Module Detail page"
    - "Module Detail form fields populate from API data and save successfully"
    - "Dragging a module row reorders the list; reloading the page confirms the new order persists"
    - "AI description generates and streams into the description field"
  artifacts:
    - path: "frontend/src/pages/creator/CourseBuilderPage.tsx"
      provides: "Verified working in browser"
    - path: "frontend/src/pages/creator/ModuleDetailPage.tsx"
      provides: "Verified working in browser"
  key_links:
    - from: "browser: /creator/courses/:id/builder"
      to: "CourseBuilderPage"
      via: "React Router SPA navigation"
      pattern: "left-rail tree visible"
    - from: "browser: /creator/courses/:id/modules/:moduleId"
      to: "ModuleDetailPage"
      via: "click from tree rail or direct URL"
      pattern: "form fields populated"
---

<objective>
Human verification of all Phase 13 BUILD requirements in the browser. All automated tests pass. This plan pauses for the creator (you) to confirm the full flow works end-to-end.

Purpose: BUILD-01 through BUILD-06 require real browser interaction to verify drag-drop persistence, SSE streaming, and navigation — things automated unit tests cannot fully cover.
Output: Human sign-off that Phase 13 is complete.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-course-builder-module-detail/13-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build check</name>
  <files></files>
  <action>
Run all automated checks before requesting human verification:

1. Frontend unit tests (all must GREEN):
   `cd frontend && npm run test:unit`

2. Backend tests (all must GREEN):
   `cd backend && python -m pytest tests/ -x -q`

3. TypeScript check (no errors):
   `cd frontend && npx tsc --noEmit`

4. Frontend build (must succeed):
   `cd frontend && npm run build`

If any check fails, fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit 2>&1 | tail -5 && cd ../backend && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All frontend tests GREEN, all backend tests GREEN, TypeScript no errors, build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 13 complete: CourseBuilderPage (left-rail tree + sortable module overview), ModuleDetailPage (form edit + AI streaming), backend SSE endpoint for module description generation, dnd-kit drag-drop reorder for modules and videos, all routes wired in App.tsx.
  </what-built>
  <how-to-verify>
Start the dev server: `cd frontend && npm run dev` and ensure backend is running.

**Check 1 — Course Builder loads (BUILD-01, BUILD-03)**
1. Log in as a creator and navigate to a course
2. Click "Open Builder" (or navigate to `/lms/creator/courses/:id/builder`)
3. Confirm: left-rail tree visible on the left with module titles
4. Confirm: each module in the tree shows a status pill (draft or published)
5. Confirm: module overview list visible in the main area with module cards

**Check 2 — Navigation from tree (BUILD-02)**
1. Click any module row in the left-rail tree
2. Confirm: navigates to `/lms/creator/courses/:id/modules/:moduleId`
3. Confirm: Module Detail page loads with the correct module's title in the form
4. Click "← Back to Course Builder"
5. Confirm: returns to Course Builder at the correct URL

**Check 3 — Module Detail form saves (BUILD-04)**
1. On Module Detail page, change the title to something unique
2. Change duration estimate to `30`
3. Change unlock rule to "After completing previous module"
4. Click "Save changes"
5. Confirm: "Saved" message appears
6. Hard reload the page (Cmd+Shift+R)
7. Confirm: the updated values are still present (persisted via API)

**Check 4 — Drag-drop reorder persists (BUILD-06)**
1. Return to Course Builder
2. Drag Module 2 above Module 1 using the drag handle (⠿ icon)
3. Confirm: modules reorder visually
4. Hard reload the page
5. Confirm: the new order is preserved (API persisted the reorder)
6. If videos are visible under a module, try dragging a video to reorder within the same module
7. Confirm: video reorder persists after reload

**Check 5 — AI description streaming (BUILD-05)**
1. Open Module Detail for any module
2. In the "Generate with AI" panel, type a topic prompt (e.g. "Python data types and variables")
3. Click "Generate description"
4. Confirm: tokens stream one-by-one into the description textarea
5. Confirm: streaming stops naturally and description field contains a full description
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass. Describe any issues found and I will fix them.</resume-signal>
</task>

</tasks>

<verification>
All automated checks green before human verify begins. Human approves all 5 browser checks.
</verification>

<success_criteria>
- Full test suite GREEN (frontend + backend)
- Build succeeds
- Human confirms: tree visible, navigation works, form saves, reorder persists, AI streams
- All 6 BUILD requirements (BUILD-01 through BUILD-06) verified in browser
</success_criteria>

<output>
After completion, create `.planning/phases/13-course-builder-module-detail/13-05-SUMMARY.md`
</output>
