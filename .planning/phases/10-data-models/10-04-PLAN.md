---
phase: 10-data-models
plan: 04
type: execute
wave: 4
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - start.sh
autonomous: false
requirements:
  - DATA-08

must_haves:
  truths:
    - "Coolify start command runs `alembic upgrade head` before `uvicorn` on every deploy"
    - "A Coolify redeploy succeeds: build logs show `alembic upgrade head` output without error"
    - "The app is accessible at buildbench.uk/lms after the deploy"
    - "`alembic current` on the production DB shows `004 (head)`"
  artifacts:
    - path: "start.sh"
      provides: "Updated local dev start script with migration step (for local dev parity)"
      contains: "alembic upgrade head"
  key_links:
    - from: "Coolify dashboard Start Command"
      to: "alembic upgrade head"
      via: "Coolify application settings → Start Command field"
      pattern: "alembic upgrade head && uvicorn"
---

<objective>
Wire `alembic upgrade head` into the Coolify deploy pipeline and verify the production migration succeeds.

Purpose: Every Coolify deploy must run migrations before starting uvicorn. Without this, the new tables will not exist in production and Phase 11 API endpoints will 500 on every request.
Output: Updated start.sh + human-verified Coolify start command + production smoke test.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-data-models/10-RESEARCH.md
@.planning/phases/10-data-models/10-03-SUMMARY.md

<interfaces>
<!-- Current start.sh (local dev only): -->
<!-- cd "$BACKEND_DIR" && python3 -m uvicorn main:app --host 0.0.0.0 --port "$PORT" --reload -->

<!-- Coolify start command (configured in Coolify dashboard, NOT a committed file): -->
<!-- Currently: cd backend && uvicorn main:app --host 0.0.0.0 --port 8000 -->
<!-- Must become: cd backend && alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000 -->

<!-- Pitfall 3 reminder: On a FRESH database (new Coolify environment), alembic upgrade head -->
<!-- runs before init_db() (which runs at uvicorn startup). This is correct — Alembic creates -->
<!-- all tables including alembic_version, then init_db()'s create_all() is a no-op. -->
<!-- No manual `alembic stamp head` step is needed because Alembic runs first. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update start.sh to include migration step</name>
  <files>start.sh</files>
  <action>
Edit `start.sh`. Find the final uvicorn launch line (currently `python3 -m uvicorn main:app ...`) and prepend the migration step. Replace the final section of start.sh from the "Starting server" echo onward with:

```bash
echo ""
echo "Running database migrations..."
cd "$BACKEND_DIR"
python3 -m alembic upgrade head
if [ $? -ne 0 ]; then
    echo "ERROR: alembic upgrade head failed. Aborting start."
    exit 1
fi

echo ""
echo "Starting server on http://localhost:$PORT"
echo "Admin login: admin / (see .env or first-run log for password)"
echo "API docs: http://localhost:$PORT/docs"
echo "Press Ctrl+C to stop"
echo ""

cd "$BACKEND_DIR"
python3 -m uvicorn main:app --host 0.0.0.0 --port "$PORT" --reload
```

Note: Use `python3 -m alembic` (not bare `alembic`) to ensure the venv-installed alembic is used regardless of PATH. This matches the existing `python3 -m uvicorn` pattern in the file.
  </action>
  <verify>
    <automated>grep -n "alembic upgrade head" "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/start.sh"</automated>
  </verify>
  <done>grep finds "alembic upgrade head" in start.sh with a line number.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Update Coolify start command and verify production migration</name>
  <action>
Human action required: Update the Coolify dashboard start command, trigger a redeploy, and verify the production migration completes successfully.

**Step 1 — Update Coolify start command:**
1. Open the Coolify dashboard for the LMS application
2. Navigate to: Application → Configuration → Start Command
3. Change the start command to:
   ```
   cd backend && alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000
   ```
4. Save the configuration

**Step 2 — Trigger a redeploy:**
1. Click "Redeploy" (or push a git commit to trigger CI)
2. Watch the build logs in Coolify
3. Confirm the logs show `alembic upgrade head` running successfully before uvicorn starts
4. Confirm no migration errors in the logs

**Step 3 — Verify production schema:**
1. Open a shell into the running Coolify container (or use Coolify's terminal feature)
2. Run: `cd backend && alembic current`
3. Confirm output shows `004 (head)`

**Step 4 — Smoke test:**
1. Visit https://buildbench.uk/lms in a browser
2. Log in as admin or creator
3. Confirm the app loads without 500 errors

**Expected:** App loads, alembic current shows 004, build logs show migration step.
  </action>
  <verify>
    <automated>echo "Manual verification required — see action steps above"</automated>
  </verify>
  <done>
    All 4 steps pass: Coolify start command updated, redeploy logs show alembic upgrade head success, `alembic current` shows `004 (head)`, app loads at buildbench.uk/lms.

    Resume signal: Type "approved" if all 4 steps pass. Or describe what you saw (e.g. "migration errored with X", "app loads but alembic current shows 003").
  </done>
</task>

</tasks>

<verification>
1. start.sh contains `alembic upgrade head` before uvicorn launch
2. Coolify build logs show migration running successfully on redeploy
3. `alembic current` on production shows `004 (head)`
4. https://buildbench.uk/lms loads without regression
</verification>

<success_criteria>
Coolify deploys automatically run `alembic upgrade head` before starting uvicorn. Production database has all 8 new tables. App is accessible and functioning at buildbench.uk/lms.
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-models/10-04-SUMMARY.md`
</output>
