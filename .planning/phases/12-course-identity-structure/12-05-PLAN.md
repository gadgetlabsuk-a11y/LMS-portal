---
phase: 12-course-identity-structure
plan: 05
type: execute
wave: 4
depends_on:
  - 12-04
files_modified: []
autonomous: false
requirements:
  - COURSE-01
  - COURSE-02
  - COURSE-03
  - COURSE-04
  - COURSE-05

must_haves:
  truths:
    - "A creator can complete the full Modal 1A → Modal 1B → builder stub flow without error"
    - "AI generation streams text visibly into the description field in real time"
    - "The skeleton tree preview updates immediately when number inputs change"
    - "After confirming structure, the browser navigates to /creator/courses/:id/builder"
    - "All 4 backend tests remain GREEN and all frontend tests pass"
  artifacts:
    - path: "backend/tests/test_courses_phase12.py"
      provides: "4 GREEN tests"
      contains: "test_create_course_with_identity, test_generate_description_sse, test_generate_objectives_sse, test_scaffold_structure"
  key_links:
    - from: "browser (creator)"
      to: "/creator/courses"
      via: "navigation"
      pattern: "CreatorCourseListPage loads, shows course list and New Course button"
---

<objective>
Human verification checkpoint — confirm the full course creation flow works in the browser before Phase 12 is marked complete.

Purpose: Automated tests cover the API contract and component logic. This checkpoint verifies the integrated user experience: streaming text appears in real time, skeleton preview responds to inputs, and scaffolding navigates correctly.
Output: Creator confirms or reports issues. Phase 12 either passes or returns to implementation plans.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-course-identity-structure/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build before handoff</name>
  <files></files>
  <action>
    Run the full backend + frontend test suite and confirm the build is clean before asking the creator to open the browser.

    ```bash
    # Backend
    cd backend && python -m pytest tests/ -x -q 2>&1 | tail -15

    # Frontend tests
    cd frontend && npm run test:unit -- --run 2>&1 | tail -10

    # Frontend build
    cd frontend && npm run build 2>&1 | tail -5
    ```

    If any test fails or the build errors, fix before proceeding to the human-verify checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend" && python -m pytest tests/ -x -q 2>&1 | tail -5 && cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/frontend" && npm run test:unit -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>Backend all GREEN. Frontend all GREEN. Build succeeds. Ready for browser verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 12 course creation flow:
    - CreatorCourseListPage at /creator/courses (replaces shared admin CourseManagementPage)
    - Modal 1A: course identity form with AI streaming for description and objectives
    - Modal 1B: structure wizard with live skeleton tree preview and scaffolding
    - /creator/courses/:id/builder stub route (Phase 13 placeholder)
  </what-built>
  <how-to-verify>
    Deploy the frontend build (or run `npm run dev` locally) and log in as a creator.

    **Check 1: Course list page loads**
    1. Navigate to /creator/courses
    2. Confirm you see the course list (existing courses shown) and a "New Course" button
    3. Confirm the admin /admin/courses route still shows CourseManagementPage without regression

    **Check 2: Modal 1A — Course Identity form**
    1. Click "New Course"
    2. Confirm the modal opens with fields: Title, Description (Textarea), Audience Level (Select), Tone Preset (Select), Learning Objectives (list with add/remove)
    3. Fill in Title: "Test Course"
    4. Select Tone Preset: "Professional"
    5. Click "Generate with AI" for Description — confirm streamed text appears character-by-character in the Description Textarea
    6. After streaming completes, you can edit the generated description
    7. Add 2 learning objectives manually
    8. Click "Generate with AI" for Objectives — confirm streamed text populates the objectives list
    9. Note: AI generation requires the backend to be running with a valid Claude API key. If the backend is not running, skip steps 5–8 and only verify the form layout.

    **Check 3: Modal 1B — Structure Wizard**
    1. Click "Create Course" in Modal 1A (fill title at minimum)
    2. Confirm Modal 1B opens automatically
    3. Change "Number of Modules" to 3 — confirm skeleton tree immediately shows 3 module rows
    4. Change "Videos per Module" to 2 — confirm each module row shows 2 video sub-rows (6 video rows total)
    5. Toggle "Include a quiz in each module" ON — confirm each module group gains a Quiz row
    6. Confirm the preview updates on every keystroke with no delay or API call

    **Check 4: Scaffolding and navigation**
    1. Click "Confirm & Build"
    2. Confirm a loading state appears (button disabled, spinner)
    3. After a few seconds (API calls), confirm navigation to /creator/courses/{id}/builder
    4. Confirm the builder stub page renders: "Course Builder / Coming in Phase 13."
    5. Navigate back to /creator/courses — confirm the new course appears in the list

    **Check 5: No regression**
    1. Confirm /admin/courses still loads CourseManagementPage without errors
    2. Confirm existing creator routes (/creator/dashboard, /creator/learners) still work
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 checks pass.
    Or describe any issues found (e.g. "streaming not working", "skeleton not updating", "scaffolding failed") and I will fix them.
  </resume-signal>
</task>

</tasks>

<verification>
```bash
# Final confirmation: all tests green, build clean
cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend" && python -m pytest tests/ -q 2>&1 | tail -5
cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/frontend" && npm run build 2>&1 | tail -3
```
</verification>

<success_criteria>
- Full test suite GREEN (backend + frontend)
- Creator confirms all 5 browser checks pass
- Phase 12 is ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/12-course-identity-structure/12-05-SUMMARY.md`
</output>
