---
phase: 12-course-identity-structure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/tests/test_courses_phase12.py
  - frontend/src/components/course/__tests__/SkeletonTreePreview.test.tsx
autonomous: true
requirements:
  - COURSE-01
  - COURSE-02
  - COURSE-03
  - COURSE-04
  - COURSE-05

must_haves:
  truths:
    - "All 5 Phase 12 test stubs exist and fail before implementation"
    - "sse-starlette is listed in backend/requirements.txt"
    - "The frontend test stub file is in the correct location for vitest to discover it"
  artifacts:
    - path: "backend/tests/test_courses_phase12.py"
      provides: "Failing stubs for COURSE-01, COURSE-02, COURSE-03, COURSE-05 backend tests"
      contains: "test_create_course_with_identity, test_generate_description_sse, test_generate_objectives_sse, test_scaffold_structure"
    - path: "frontend/src/components/course/__tests__/SkeletonTreePreview.test.tsx"
      provides: "Failing stub for COURSE-04 frontend test"
      contains: "renders skeleton tree"
    - path: "backend/requirements.txt"
      provides: "sse-starlette dependency declaration"
      contains: "sse-starlette"
  key_links:
    - from: "backend/tests/test_courses_phase12.py"
      to: "backend/routers/courses.py"
      via: "HTTP client test calls"
      pattern: "client.post.*api/courses"
    - from: "frontend/src/components/course/__tests__/SkeletonTreePreview.test.tsx"
      to: "frontend/src/components/course/SkeletonTreePreview.tsx"
      via: "vitest import"
      pattern: "import.*SkeletonTreePreview"
---

<objective>
Create failing test stubs for all Phase 12 requirements and add sse-starlette to requirements.txt.

Purpose: Establish the Nyquist-compliant test scaffold before any implementation begins. Tests must fail (RED) now and turn GREEN as each implementation plan completes.
Output: Three files — backend test stubs, frontend test stub, updated requirements.txt.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-course-identity-structure/12-RESEARCH.md
@.planning/phases/12-course-identity-structure/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sse-starlette to requirements.txt</name>
  <files>backend/requirements.txt</files>
  <action>
    Open backend/requirements.txt and append the following line:

    ```
    sse-starlette==2.1.3
    ```

    Place it near the other async/streaming dependencies (after httpx). Do not remove or reorder existing entries.
  </action>
  <verify>
    <automated>grep "sse-starlette" "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend/requirements.txt"</automated>
  </verify>
  <done>requirements.txt contains exactly one line matching "sse-starlette==2.1.3"</done>
</task>

<task type="auto">
  <name>Task 2: Create backend test stubs (COURSE-01, COURSE-02, COURSE-03, COURSE-05)</name>
  <files>backend/tests/test_courses_phase12.py</files>
  <action>
    Create backend/tests/test_courses_phase12.py with four stub tests that each call pytest.fail() immediately. This makes them RED now and GREEN once implementation plans complete.

    The conftest.py in backend/tests/ already provides: `client` (TestClient), `creator_token` (JWT for a creator), `admin_token`, and `creator_course` (a Course object). Import from conftest via pytest fixtures.

    The bcrypt monkey-patch pattern is already in conftest.py (from STATE.md decisions 11-04). Do not add it again.

    ```python
    """
    Phase 12 test stubs — Course Identity & Structure.
    All tests start as FAILING stubs (Wave 0).
    Replace pytest.fail() with real assertions in Wave 1.
    """
    import pytest


    def test_create_course_with_identity(client, creator_token):
        """COURSE-01: POST /api/courses saves audience_level, learning_objectives, ai_tone_preset."""
        pytest.fail("STUB — implement in 12-02")


    def test_generate_description_sse(client, creator_token):
        """COURSE-02: POST /api/courses/ai/generate-description returns SSE text/event-stream."""
        pytest.fail("STUB — implement in 12-02")


    def test_generate_objectives_sse(client, creator_token):
        """COURSE-03: POST /api/courses/ai/generate-objectives returns SSE text/event-stream."""
        pytest.fail("STUB — implement in 12-02")


    def test_scaffold_structure(client, creator_token, creator_course):
        """COURSE-05: Scaffolding creates expected modules and videos in order."""
        pytest.fail("STUB — implement in 12-02")
    ```

    Note: These stubs do not import any Phase 12 implementation code — they will fail immediately via pytest.fail(), not via ImportError. This ensures they fail cleanly before courses.py is changed.
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend" && python -m pytest tests/test_courses_phase12.py -v 2>&1 | grep -E "FAILED|PASSED|ERROR|4 failed"</automated>
  </verify>
  <done>All 4 tests in test_courses_phase12.py show as FAILED (not ERROR) when pytest runs. No ImportError.</done>
</task>

<task type="auto">
  <name>Task 3: Create frontend SkeletonTreePreview test stub (COURSE-04)</name>
  <files>frontend/src/components/course/__tests__/SkeletonTreePreview.test.tsx</files>
  <action>
    Create the directory frontend/src/components/course/__tests__/ if it does not exist, then create the stub test file.

    The test imports SkeletonTreePreview which does not exist yet, so the import will fail. To make the test fail cleanly (not with an unresolvable import), use a lazy pattern: write a test that will fail once the component exists but not cause a build-time crash now.

    Actually: vitest fails the test file entirely on unresolvable import, which counts as a test failure (not a collection error that silences other tests). That is acceptable for Wave 0 — the stub IS failing.

    ```tsx
    /**
     * Phase 12 stub — SkeletonTreePreview (COURSE-04).
     * This test will fail with import error until 12-03 creates the component.
     */
    import { describe, it, expect } from 'vitest'
    import { render } from '@testing-library/react'
    import { SkeletonTreePreview } from '../SkeletonTreePreview'

    describe('SkeletonTreePreview', () => {
      it('renders skeleton tree nodes for given module and video counts', () => {
        const { getAllByRole } = render(
          <SkeletonTreePreview moduleCount={2} videosPerModule={2} quizPerModule={false} />
        )
        // 2 modules + 4 videos = 6 tree items
        expect(getAllByRole('listitem').length).toBe(6)
      })

      it('includes quiz nodes when quizPerModule is true', () => {
        const { getAllByText } = render(
          <SkeletonTreePreview moduleCount={1} videosPerModule={1} quizPerModule={true} />
        )
        expect(getAllByText(/Quiz/i).length).toBe(1)
      })
    })
    ```

    The component file `frontend/src/components/course/SkeletonTreePreview.tsx` does not exist yet. Vitest will fail this test file at import, which is the intended RED state.
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/frontend" && npm run test:unit -- --run 2>&1 | grep -E "SkeletonTreePreview|FAIL|fail" | head -5</automated>
  </verify>
  <done>Running `npm run test:unit -- --run` shows SkeletonTreePreview.test.tsx as FAILED or error (not passing). Backend stubs also failing. Wave 0 is RED.</done>
</task>

</tasks>

<verification>
```bash
# Backend stubs: all 4 must FAIL
cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend" && python -m pytest tests/test_courses_phase12.py -v 2>&1 | tail -10

# Frontend stub: must show failure
cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/frontend" && npm run test:unit -- --run 2>&1 | grep -E "SkeletonTreePreview|fail" | head -5

# sse-starlette in requirements
grep "sse-starlette" "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend/requirements.txt"
```
</verification>

<success_criteria>
- backend/tests/test_courses_phase12.py exists with 4 stub tests, all FAILED
- frontend/src/components/course/__tests__/SkeletonTreePreview.test.tsx exists and fails
- backend/requirements.txt contains sse-starlette==2.1.3
- No other existing tests are broken by these additions
</success_criteria>

<output>
After completion, create `.planning/phases/12-course-identity-structure/12-01-SUMMARY.md`
</output>
