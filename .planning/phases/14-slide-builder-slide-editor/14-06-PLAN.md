---
phase: 14-slide-builder-slide-editor
plan: 06
type: execute
wave: 5
depends_on:
  - 14-05
files_modified: []
autonomous: false
requirements:
  - SLIDE-01
  - SLIDE-02
  - SLIDE-03
  - SLIDE-04
  - SLIDE-05
  - SLIDE-06
  - SLIDE-07
  - SLIDE-08
  - SLIDE-09
  - SLIDE-10
  - SLIDE-11
  - SLIDE-12

must_haves:
  truths:
    - "Creator can see slide thumbnail strip for a video"
    - "Creator can add, reorder, duplicate, and delete slides from the strip"
    - "Creator can drag blocks from the palette onto the 12-column snap-grid canvas"
    - "Creator can resize and reposition blocks; changes persist after page reload"
    - "Creator can undo and redo at least 20 canvas changes"
    - "Saved indicator appears after canvas edits; navigation away flushes pending save"
    - "Creator can select a layout preset and canvas updates to match"
    - "Creator can write a narration script and see it persist"
    - "Creator can generate AI narration and see tokens stream into the textarea"
    - "Creator can complete all 4 steps of the slide outline wizard and see new slides appear in the strip"
  artifacts:
    - path: "All Phase 14 implementation files"
      provides: "Full slide authoring experience"
  key_links:
    - from: "Browser"
      to: "Deployed site"
      via: "Manual walkthrough of all 6 success criteria"
      pattern: "human-verify"
---

<objective>
Human verification: browser walkthrough of all 6 Phase 14 success criteria.

Purpose: Confirms the complete slide authoring experience works end-to-end in a real browser with real backend.
Output: Human approval or specific issues to fix.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm all green</name>
  <files></files>
  <action>
    Run the complete test suite before presenting to human:

    ```bash
    cd frontend && npm run test:unit 2>&1 | tail -20
    cd backend && source venv/bin/activate && python -m pytest tests/ -x -q 2>&1 | tail -15
    ```

    If any tests fail, do not proceed to the checkpoint — fix failures first.
    List which tests pass/fail in the summary before the checkpoint.
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit 2>&1 | grep -E "passed|failed|Tests" | tail -5</automated>
  </verify>
  <done>All frontend and backend tests pass with zero failures</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 14 complete implementation:
    - SlideBuilderPage: thumbnail strip with add/reorder/duplicate/delete + bulk narration button (disabled)
    - SlideEditorPage: react-grid-layout 12-column canvas + block palette + layout presets + undo/redo + autosave + narration tab
    - AI narration generation (SSE streaming into textarea)
    - 4-step slide outline wizard
    - Two new App.tsx routes wired under CreatorLayout + ProtectedRoute
  </what-built>
  <how-to-verify>
    Deploy or run locally (`cd frontend && npm run dev` + `cd backend && uvicorn main:app --reload`).

    **Check 1 — Slide Builder & strip (SLIDE-01, SLIDE-02):**
    1. Log in as a creator. Navigate to a video of type `slideshow_narrated` in the Course Builder.
    2. Visit `/creator/courses/{id}/videos/{videoId}/slides`
    3. Confirm: thumbnail strip is visible with any existing slides
    4. Click "+ Add Slide" — confirm new slide appears in strip
    5. Drag a slide thumbnail to reorder — confirm order updates and persists after page reload
    6. Click the Duplicate button (⧉) on a slide — confirm a copy appears
    7. Click the Delete button (✕) on a slide — confirm it disappears

    **Check 2 — Bulk narration button (SLIDE-03):**
    8. Confirm "Generate Narration" button is visible in the toolbar
    9. Confirm it is disabled and shows tooltip "Audio generation available in a future update"

    **Check 3 — Slide Editor & canvas (SLIDE-04, SLIDE-05, SLIDE-06):**
    10. Click a slide thumbnail to open `/creator/courses/{id}/videos/{videoId}/slides/{slideId}/editor`
    11. Confirm the canvas renders with the 12-column grid (block boundaries snap to grid)
    12. Click "Text" in the block palette — confirm a text block appears on canvas
    13. Drag the block to a new position — confirm it stays where you drop it
    14. Resize the block using the resize handle — confirm new size is saved after drag

    **Check 4 — Undo/redo (SLIDE-07):**
    15. Add 3 blocks to the canvas
    16. Click Undo 3 times — confirm canvas returns to empty
    17. Click Redo 3 times — confirm all 3 blocks restored

    **Check 5 — Autosave (SLIDE-08):**
    18. Edit a block's text content
    19. Wait 1 second — confirm "Saved" indicator appears
    20. Edit content again, immediately click "← Slides" — confirm navigation proceeds without data loss

    **Check 6 — Layout presets (SLIDE-09):**
    21. Click the "Layout" tab in the right panel
    22. Click "Two Column" preset — confirm canvas clears and shows two side-by-side blocks

    **Check 7 — Narration tab (SLIDE-10, SLIDE-11):**
    23. Click the "Narration" tab
    24. Type a narration script — confirm it saves on blur
    25. Click "Generate" — confirm tokens stream into the textarea in real time

    **Check 8 — Slide outline wizard (SLIDE-12):**
    26. Go back to SlideBuilderPage
    27. Look for an "AI Outline" or wizard trigger button (may need to add if not yet wired to SlideBuilderPage)
    28. Complete all 4 wizard steps with a topic prompt
    29. Click "Add slides" — confirm new slides appear in the thumbnail strip
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 8 checks pass.
    Or describe which checks failed with specific error messages or screenshots.
  </resume-signal>
</task>

</tasks>

<verification>
All 6 Phase 14 success criteria confirmed by human in browser:
1. Thumbnail strip with add/reorder/duplicate/delete
2. Block palette → 12-column snap-grid canvas
3. Resize/reposition + undo/redo 20 steps + "Saved" indicator
4. Layout preset replaces canvas blocks
5. Narration tab + AI narration streaming
6. 4-step slide outline wizard → slides committed to strip
</verification>

<success_criteria>
Human approves all 8 browser checks. Phase 14 COMPLETE.
All SLIDE-01 through SLIDE-12 requirements verified in browser.
</success_criteria>

<output>
After approval, create `.planning/phases/14-slide-builder-slide-editor/14-06-SUMMARY.md`
Update STATE.md: Phase 14 COMPLETE, next phase is 15.
</output>
