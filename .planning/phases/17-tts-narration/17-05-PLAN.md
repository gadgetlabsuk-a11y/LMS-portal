---
phase: 17-tts-narration
plan: 05
type: execute
wave: 3
depends_on: [17-03, 17-04]
files_modified: []
autonomous: false
requirements: [TTS-01, TTS-02, TTS-03, TTS-04, TTS-05]

must_haves:
  truths:
    - "Creator can generate audio for a single slide and hear it in the NarrationTab audio player"
    - "Creator can trigger bulk generation from SlideBuilderPage and see result counts"
    - "Running bulk twice skips slides whose scripts have not changed"
    - "Creator can select Rachel or Josh voice before generating"
    - "All 5 backend tests and frontend test suite are green before human verification"
  artifacts:
    - path: "backend/tests/test_tts_phase17.py"
      provides: "5/5 green tests"
    - path: "frontend/src/components/slide/__tests__/NarrationTab.test.tsx"
      provides: "5/5 green tests"
  key_links:
    - from: "NarrationTab audio player"
      to: "ElevenLabs MP3 file"
      via: "API_BASE + narration_audio_url served from uploads/audio/"
      pattern: "API_BASE.*narration_audio_url"
---

<objective>
Human verification: confirm all TTS & Narration features work end-to-end with a live ElevenLabs API key.

Purpose: Gate the phase on human confirmation that audio generates, plays, caching works, voice selection works, and bulk skipping is correct. Cannot be automated — requires live ElevenLabs API and browser.
Output: Phase 17 signed off. SLIDE-03 closed. TTS-01 through TTS-05 all verified.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-tts-narration/17-03-SUMMARY.md
@.planning/phases/17-tts-narration/17-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm all green</name>
  <files></files>
  <action>
Run all tests before invoking the human checkpoint to ensure there are no regressions:

```bash
# Backend — all TTS tests and full suite
cd backend && source venv/bin/activate
python -m pytest tests/test_tts_phase17.py -v
python -m pytest tests/ -x -q

# Frontend — full suite
cd frontend && npm run test:unit
```

Fix any failures before proceeding to the human checkpoint. Do not invoke the checkpoint if the automated suite has failures.
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend" &amp;&amp; source venv/bin/activate &amp;&amp; python -m pytest tests/test_tts_phase17.py -q 2>&amp;1 | tail -5 &amp;&amp; cd ../frontend &amp;&amp; npx vitest run 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Backend: 5/5 TTS tests pass. Full backend suite green. Frontend: all tests pass including 5 NarrationTab tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — 5 browser checks (TTS-01 through TTS-05)</name>
  <action>Human verifies all 5 checks described below in the browser with live dev servers running and ELEVENLABS_API_KEY set in .env.</action>
  <what-built>
Phase 17 TTS & Narration features:
1. Per-slide audio generation via ElevenLabs (TTS-01) — NarrationTab "Generate audio" button → audio player
2. Bulk audio generation for all slides in a video (TTS-02) — SlideBuilderPage "Generate Narration" button
3. Semaphore rate limiting prevents API storms (TTS-03) — verified via bulk behaviour
4. Script-hash caching prevents redundant regeneration (TTS-04) — run bulk twice to confirm
5. Voice selection (Rachel / Josh) stored and used in generation (TTS-05)
6. SLIDE-03 closed: bulk narration button is now wired (was disabled since Phase 14)
  </what-built>
  <how-to-verify>
Ensure ELEVENLABS_API_KEY is set in your .env file before starting.

**Check 1 — Per-slide audio generation (TTS-01):**
1. Open a course, navigate to a video's Slide Editor
2. Open the Narration tab on any slide
3. Write or generate a narration script (at least 5 words)
4. Click "Generate audio"
5. Confirm: button shows "Generating..." while in progress
6. Confirm: audio player (audio element with controls) appears below the voice selector
7. Confirm: audio plays (click the play button) and it sounds like the narration text

**Check 2 — Voice selection (TTS-05):**
1. In NarrationTab, change the voice dropdown from Rachel to Josh
2. Click "Generate audio"
3. Confirm: audio sounds noticeably different from Rachel

**Check 3 — Bulk generation (TTS-02):**
1. Navigate to SlideBuilderPage for a video with at least 3 slides
2. Ensure at least 2 slides have narration scripts (edit in Narration tab)
3. Click "Generate Narration" (previously disabled bulk button)
4. Confirm: button shows "Generating..." while running
5. Confirm: result banner appears: "Generated X audio file(s). Skipped Y (no script)."
6. Confirm: slides with no script are counted in the skipped count

**Check 4 — Cache / skip unchanged slides (TTS-04):**
1. Run bulk generation for the same video again WITHOUT editing any scripts
2. Confirm: result banner shows "Cached X (unchanged)" and generated=0
3. Edit one slide's narration script slightly
4. Run bulk again
5. Confirm: only that one edited slide shows generated=1, remaining show cached

**Check 5 — 503 graceful degradation:**
1. Temporarily remove ELEVENLABS_API_KEY from .env and restart backend
2. Click "Generate audio" on any slide with a script
3. Confirm: error message appears ("Audio generation failed" or similar)
4. Restore the API key and restart backend before finishing
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe which check failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Human-verified: all 5 checks pass and creator reported "approved".
Backend + frontend automated suites both green.
</verification>

<success_criteria>
Phase 17 complete when:
- All 5 browser checks approved by user
- test_tts_phase17.py: 5/5 passing
- NarrationTab.test.tsx: 5/5 passing (3 SLIDE-10/11 + 2 TTS-01)
- Full backend suite passes with no regressions
- Full frontend suite passes with no regressions
- SLIDE-03 confirmed closed in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/17-tts-narration/17-05-SUMMARY.md`

Also update REQUIREMENTS.md: check off SLIDE-03 (bulk narration button — deferred from Phase 14, implemented in Phase 17 Plan 04).
</output>
