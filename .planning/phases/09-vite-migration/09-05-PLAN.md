---
phase: 09-vite-migration
plan: 05
type: execute
wave: 4
depends_on:
  - 09-04
files_modified:
  - frontend/src/pages/admin/AdminDashboard.tsx
  - frontend/src/pages/admin/UserManagementPage.tsx
  - frontend/src/pages/admin/CourseManagementPage.tsx
  - frontend/src/pages/admin/SecurityPage.tsx
  - frontend/src/pages/admin/DevToolsPage.tsx
  - frontend/src/pages/admin/WhiteLabelPage.tsx
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Admin dashboard loads and displays stats cards (users, courses, active sessions)"
    - "User management table loads users from GET /api/admin/users with pagination"
    - "Course management loads courses and supports create/delete actions"
    - "Security page loads security settings and supports save"
    - "Dev Tools page renders tool panels (db stats, cache clear, etc.)"
    - "White Label page loads brand settings and saves via PUT /api/whitelabel"
  artifacts:
    - path: "frontend/src/pages/admin/AdminDashboard.tsx"
      exports: ["AdminDashboard"]
    - path: "frontend/src/pages/admin/UserManagementPage.tsx"
      exports: ["UserManagementPage"]
    - path: "frontend/src/pages/admin/CourseManagementPage.tsx"
      exports: ["CourseManagementPage"]
    - path: "frontend/src/pages/admin/SecurityPage.tsx"
      exports: ["SecurityPage"]
    - path: "frontend/src/pages/admin/DevToolsPage.tsx"
      exports: ["DevToolsPage"]
    - path: "frontend/src/pages/admin/WhiteLabelPage.tsx"
      exports: ["WhiteLabelPage"]
  key_links:
    - from: "all admin pages"
      to: "api service"
      via: "import { api } from '@/services/api'"
      pattern: "api\\.get|api\\.post|api\\.put|api\\.delete"
    - from: "all admin pages"
      to: "useToast"
      via: "import { useToast } from '@/context/ToastContext'"
      pattern: "showToast"
---

<objective>
Extract the 6 admin page components from the monolith. These are Wave 4 and can run in parallel with Plan 06 (creator/learner pages) because they touch different files.

Purpose: After this plan, the /admin/* route tree is fully functional.
Output: 6 TypeScript page components in frontend/src/pages/admin/
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-vite-migration/09-04-SUMMARY.md
@.planning/phases/09-vite-migration/09-RESEARCH.md

<interfaces>
<!-- From Plan 02: api service -->
import { api } from '@/services/api'
// api.get(path) → Promise<Response>
// api.post(path, body) → Promise<Response>
// api.put(path, body) → Promise<Response>
// api.delete(path) → Promise<Response>

<!-- From Plan 03: common components -->
import { Modal } from '@/components/common/Modal'
import { Button } from '@/components/common/Button'
import { Card } from '@/components/common/Card'
import { Badge } from '@/components/common/Badge'
import { Input } from '@/components/common/Input'
import { Select } from '@/components/common/Select'
import { Textarea } from '@/components/common/Textarea'

<!-- From Plan 02: contexts -->
import { useAuth } from '@/context/AuthContext'
import { useToast } from '@/context/ToastContext'

<!-- Note: CourseManagementPage is used by both /admin/courses AND /creator/courses routes.
     It is placed in src/pages/admin/ but must not contain any logic that assumes admin role.
     Creator routes also render this component inside CreatorLayout. Check monolith for any
     role-conditional rendering and preserve it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AdminDashboard and UserManagementPage</name>
  <files>
    frontend/src/pages/admin/AdminDashboard.tsx,
    frontend/src/pages/admin/UserManagementPage.tsx
  </files>
  <action>
Read `frontend/index.html.monolith.bak` to locate AdminDashboard and UserManagementPage component definitions. Search for `const AdminDashboard` and `const UserManagementPage` in the file.

For each component:
1. Copy the complete JSX body
2. Add TypeScript interface for any local state
3. Replace `api.get/post/put/delete` calls — these are already compatible (same function signatures)
4. Replace `showToast` — import from `@/context/ToastContext`
5. Replace all inline arrow components that reference common components — import from `@/components/common/`
6. Add `export const` prefix

**AdminDashboard.tsx** structure (from monolith):
- Loads stats from API on mount (user count, course count, active sessions, etc.)
- Renders stat cards in a grid
- Shows recent activity or audit log entries
- Uses `api.get('/admin/stats')` or similar endpoint

**UserManagementPage.tsx** structure (from monolith):
- Loads users list from `api.get('/admin/users')`
- Supports create user (Modal with form), edit user role, delete user
- Pagination or full list
- Role badge display using Badge component

Both files follow this import pattern:
```typescript
import { useState, useEffect } from 'react'
import { api } from '@/services/api'
import { useToast } from '@/context/ToastContext'
import { Modal } from '@/components/common/Modal'
import { Button } from '@/components/common/Button'
import { Card } from '@/components/common/Card'
import { Badge } from '@/components/common/Badge'
import { Input } from '@/components/common/Input'
import { Select } from '@/components/common/Select'
```

Extract faithfully — do not simplify or remove functionality. Add TypeScript types for all state variables and API response shapes.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -8</automated>
  </verify>
  <done>
    - Both files compile with no TypeScript errors
    - AdminDashboard and UserManagementPage exported as named exports
    - No inline component definitions for Modal, Button, etc. (all imported from common/)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract CourseManagementPage, SecurityPage, DevToolsPage, WhiteLabelPage</name>
  <files>
    frontend/src/pages/admin/CourseManagementPage.tsx,
    frontend/src/pages/admin/SecurityPage.tsx,
    frontend/src/pages/admin/DevToolsPage.tsx,
    frontend/src/pages/admin/WhiteLabelPage.tsx
  </files>
  <action>
Read `frontend/index.html.monolith.bak` to locate each component definition. Search for `const CourseManagementPage`, `const SecurityPage`, `const DevToolsPage`, `const WhiteLabelPage`.

Extract each component to its file with the same approach as Task 1:
1. Copy complete JSX body from monolith
2. Add TypeScript types for state
3. Import api, useToast, common components from their new locations
4. Add `export const` prefix

**Special note on CourseManagementPage**: This component is rendered in BOTH AdminLayout (at /admin/courses) and CreatorLayout (at /creator/courses). Check the monolith for any role-conditional rendering inside CourseManagementPage (e.g. `if (user?.role === 'admin') { ... }`). Preserve this logic exactly — do not make it admin-only. Place the file in `src/pages/admin/` but it will be imported by both admin and creator route definitions.

**CourseManagementPage** structure:
- Loads courses from API
- Create/edit/delete course actions
- AI generation option for course content
- Uses Modal for create/edit forms

**SecurityPage** structure:
- MFA settings (enable/disable TOTP)
- Session management
- Security audit log or settings

**DevToolsPage** structure:
- Database stats
- Cache management
- System health indicators
- Development utilities

**WhiteLabelPage** structure:
- Brand name, logo URL, color theme inputs
- Preview of branding
- Save via `api.put('/whitelabel', body)`

All 4 files follow same import pattern as Task 1. Add `useAuth` import if any file uses `user.role`.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10 && npm run test:unit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 4 files compile with no TypeScript errors
    - CourseManagementPage has no hard-coded admin-only checks that would break /creator/courses
    - WhiteLabelPage uses `api.put('/whitelabel', body)` (not a hardcoded URL)
    - `npm run test:unit` still passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract safeDesc helper and any admin-specific utilities</name>
  <files>
    frontend/src/utils/text.ts
  </files>
  <action>
The monolith defines a `safeDesc` helper (lines 249–258) used across multiple page components. Extract it to a shared utility module.

**frontend/src/utils/text.ts**:
```typescript
/**
 * Strip markdown code fences / raw JSON from descriptions before display.
 * Migrated from frontend/index.html.monolith.bak lines 249–258.
 */
export const safeDesc = (text: string | null | undefined, maxLen = 160): string => {
  if (!text) return ''
  let t = text.trim()
  // Strip ```json ... ``` or ``` ... ``` blocks
  t = t.replace(/^```(?:json)?\s*/i, '').replace(/```[\s\S]*$/, '').trim()
  // If what remains looks like raw JSON, don't show it
  if (t.startsWith('{') || t.startsWith('[')) return 'Course content generated.'
  return t.length > maxLen ? t.slice(0, maxLen) + '…' : t
}
```

Update any admin page files from Tasks 1 and 2 that call `safeDesc(...)` to import from `@/utils/text`.

After creating this file, verify all admin pages that use safeDesc now import it from `@/utils/text` rather than expecting it as a global function.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `frontend/src/utils/text.ts` exists with exported `safeDesc`
    - No admin page has `safeDesc` defined inline
    - Build clean
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` exits 0
- All 6 admin page files exist at target paths
- `grep -r "window.history\|window.location.href" frontend/src/pages/admin/` returns no matches
- `npm run test:unit` passes
</verification>

<success_criteria>
1. All 6 admin pages extracted as typed TypeScript components
2. CourseManagementPage works for both admin and creator routes (no admin-only hardcoding)
3. safeDesc helper extracted to src/utils/text.ts and imported where used
4. All API calls use the api service from @/services/api
5. Build clean — no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-vite-migration/09-05-SUMMARY.md`
</output>
