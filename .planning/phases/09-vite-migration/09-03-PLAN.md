---
phase: 09-vite-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - frontend/src/components/common/Modal.tsx
  - frontend/src/components/common/Button.tsx
  - frontend/src/components/common/Card.tsx
  - frontend/src/components/common/Badge.tsx
  - frontend/src/components/common/Input.tsx
  - frontend/src/components/common/Select.tsx
  - frontend/src/components/common/Textarea.tsx
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "All 7 common components render without runtime errors"
    - "Modal renders children only when open=true, calls onClose when close button clicked"
    - "Button applies the correct Tailwind variant classes for primary/secondary/danger/ghost"
    - "Badge applies correct badge class variant (info/success/warning/danger)"
    - "Input and Textarea show error message and red border when error prop is set"
    - "Select renders an option for each entry in the options array"
  artifacts:
    - path: "frontend/src/components/common/Modal.tsx"
      exports: ["Modal"]
    - path: "frontend/src/components/common/Button.tsx"
      exports: ["Button"]
    - path: "frontend/src/components/common/Card.tsx"
      exports: ["Card"]
    - path: "frontend/src/components/common/Badge.tsx"
      exports: ["Badge"]
    - path: "frontend/src/components/common/Input.tsx"
      exports: ["Input"]
    - path: "frontend/src/components/common/Select.tsx"
      exports: ["Select"]
    - path: "frontend/src/components/common/Textarea.tsx"
      exports: ["Textarea"]
  key_links:
    - from: "src/components/common/*"
      to: "pages and layout components (Plans 04–06)"
      via: "named imports: import { Modal } from '@/components/common/Modal'"
      pattern: "@/components/common"
---

<objective>
Extract the 7 reusable UI components (Modal, Button, Card, Badge, Input, Select, Textarea) from the monolith into individual TypeScript files. These have no dependencies on AuthContext or the API — they are pure presentational components that all page and layout files import.

Purpose: Can run in parallel with Plan 02 (Wave 2). No AuthContext dependency.
Output: 7 typed React component files in frontend/src/components/common/
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-vite-migration/09-RESEARCH.md

<interfaces>
<!-- Source: frontend/index.html.monolith.bak lines 457–559 -->

Modal props: { open: boolean, onClose: () => void, title: string, children: ReactNode, size?: 'sm'|'md'|'lg'|'xl' }
  - size classes: sm=max-w-sm, md=max-w-md, lg=max-w-lg, xl=max-w-2xl
  - Returns null when open=false

Button props: { children: ReactNode, variant?: 'primary'|'secondary'|'danger'|'ghost', size?: 'sm'|'md'|'lg', fullWidth?: boolean, disabled?: boolean, ...rest }
  - variant classes: primary=bg-blue-600 text-white, secondary=bg-gray-200 text-gray-900, danger=bg-red-600 text-white, ghost=text-blue-600 hover:bg-blue-50
  - size classes: sm=px-3 py-1.5 text-sm, md=px-4 py-2 text-base, lg=px-6 py-3 text-lg

Card props: { children: ReactNode, className?: string }
  - className: bg-white rounded-lg shadow-sm border border-gray-200 + prop className

Badge props: { children: ReactNode, variant?: 'info'|'success'|'warning'|'danger' }
  - Uses CSS class .badge.{variant} defined in globals.css

Input props: { label?: string, error?: string, ...HTMLInputProps }
  - Red border on error; error message below input

Select props: { label?: string, options: {value: string, label: string}[], error?: string, ...HTMLSelectProps }
  - Includes "-- Select --" default option

Textarea props: { label?: string, error?: string, ...HTMLTextareaProps }
  - Uses .custom-textarea CSS class (monospace font, resize:vertical)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Modal, Button, Card, Badge</name>
  <files>
    frontend/src/components/common/Modal.tsx,
    frontend/src/components/common/Button.tsx,
    frontend/src/components/common/Card.tsx,
    frontend/src/components/common/Badge.tsx
  </files>
  <action>
Create each file with exact TypeScript types matching the monolith's prop shapes.

**frontend/src/components/common/Modal.tsx**:
```typescript
import { ReactNode } from 'react'

interface ModalProps {
  open: boolean
  onClose: () => void
  title: string
  children: ReactNode
  size?: 'sm' | 'md' | 'lg' | 'xl'
}

const sizeClasses = {
  sm: 'max-w-sm',
  md: 'max-w-md',
  lg: 'max-w-lg',
  xl: 'max-w-2xl',
}

export const Modal = ({ open, onClose, title, children, size = 'md' }: ModalProps) => {
  if (!open) return null

  return (
    <div className="fixed inset-0 modal-overlay flex items-center justify-center z-50 fade-in">
      <div className={`bg-white rounded-lg shadow-xl ${sizeClasses[size]} w-full mx-4`}>
        <div className="flex justify-between items-center p-6 border-b">
          <h2 className="text-xl font-bold">{title}</h2>
          <button onClick={onClose} className="text-gray-500 hover:text-gray-700">
            ✕
          </button>
        </div>
        <div className="p-6">
          {children}
        </div>
      </div>
    </div>
  )
}
```

**frontend/src/components/common/Button.tsx**:
```typescript
import { ButtonHTMLAttributes, ReactNode } from 'react'

type ButtonVariant = 'primary' | 'secondary' | 'danger' | 'ghost'
type ButtonSize = 'sm' | 'md' | 'lg'

interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  children: ReactNode
  variant?: ButtonVariant
  size?: ButtonSize
  fullWidth?: boolean
}

const variantClasses: Record<ButtonVariant, string> = {
  primary: 'bg-blue-600 text-white hover:bg-blue-700 focus:ring-blue-500',
  secondary: 'bg-gray-200 text-gray-900 hover:bg-gray-300 focus:ring-gray-400',
  danger: 'bg-red-600 text-white hover:bg-red-700 focus:ring-red-500',
  ghost: 'text-blue-600 hover:bg-blue-50 focus:ring-blue-500',
}

const sizeClasses: Record<ButtonSize, string> = {
  sm: 'px-3 py-1.5 text-sm',
  md: 'px-4 py-2 text-base',
  lg: 'px-6 py-3 text-lg',
}

export const Button = ({
  children,
  variant = 'primary',
  size = 'md',
  fullWidth = false,
  disabled = false,
  className = '',
  ...props
}: ButtonProps) => {
  const base = 'font-medium rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2'
  return (
    <button
      className={`${base} ${sizeClasses[size]} ${variantClasses[variant]} ${fullWidth ? 'w-full' : ''} ${disabled ? 'opacity-50 cursor-not-allowed' : ''} ${className}`}
      disabled={disabled}
      {...props}
    >
      {children}
    </button>
  )
}
```

**frontend/src/components/common/Card.tsx**:
```typescript
import { ReactNode } from 'react'

interface CardProps {
  children: ReactNode
  className?: string
}

export const Card = ({ children, className = '' }: CardProps) => (
  <div className={`bg-white rounded-lg shadow-sm border border-gray-200 ${className}`}>
    {children}
  </div>
)
```

**frontend/src/components/common/Badge.tsx**:
```typescript
import { ReactNode } from 'react'

type BadgeVariant = 'info' | 'success' | 'warning' | 'danger'

interface BadgeProps {
  children: ReactNode
  variant?: BadgeVariant
}

export const Badge = ({ children, variant = 'info' }: BadgeProps) => (
  <span className={`badge ${variant}`}>
    {children}
  </span>
)
```
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 4 files exist with exported named components
    - TypeScript compiles with no errors
    - No `any` types used
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Input, Select, Textarea</name>
  <files>
    frontend/src/components/common/Input.tsx,
    frontend/src/components/common/Select.tsx,
    frontend/src/components/common/Textarea.tsx
  </files>
  <action>
**frontend/src/components/common/Input.tsx**:
```typescript
import { InputHTMLAttributes } from 'react'

interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
  label?: string
  error?: string
}

export const Input = ({ label, error, ...props }: InputProps) => (
  <div className="mb-4">
    {label && (
      <label className="block text-sm font-medium mb-1 text-gray-700">{label}</label>
    )}
    <input
      {...props}
      className={`w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 ${
        error ? 'border-red-500' : 'border-gray-300'
      }`}
    />
    {error && <p className="text-red-500 text-sm mt-1">{error}</p>}
  </div>
)
```

**frontend/src/components/common/Select.tsx**:
```typescript
import { SelectHTMLAttributes } from 'react'

interface SelectOption {
  value: string
  label: string
}

interface SelectProps extends SelectHTMLAttributes<HTMLSelectElement> {
  label?: string
  options: SelectOption[]
  error?: string
}

export const Select = ({ label, options, error, ...props }: SelectProps) => (
  <div className="mb-4">
    {label && (
      <label className="block text-sm font-medium mb-1 text-gray-700">{label}</label>
    )}
    <select
      {...props}
      className={`w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 ${
        error ? 'border-red-500' : 'border-gray-300'
      }`}
    >
      <option value="">-- Select --</option>
      {options.map(opt => (
        <option key={opt.value} value={opt.value}>
          {opt.label}
        </option>
      ))}
    </select>
    {error && <p className="text-red-500 text-sm mt-1">{error}</p>}
  </div>
)
```

**frontend/src/components/common/Textarea.tsx**:
```typescript
import { TextareaHTMLAttributes } from 'react'

interface TextareaProps extends TextareaHTMLAttributes<HTMLTextAreaElement> {
  label?: string
  error?: string
}

export const Textarea = ({ label, error, ...props }: TextareaProps) => (
  <div className="mb-4">
    {label && (
      <label className="block text-sm font-medium mb-1 text-gray-700">{label}</label>
    )}
    <textarea
      {...props}
      className={`custom-textarea w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 ${
        error ? 'border-red-500' : 'border-gray-300'
      }`}
    />
    {error && <p className="text-red-500 text-sm mt-1">{error}</p>}
  </div>
)
```
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5 && npm run test:unit 2>&1 | tail -8</automated>
  </verify>
  <done>
    - All 3 files exist with exported named components
    - TypeScript compiles with no errors
    - `npm run test:unit` still passes (no regressions)
    - Each component uses the exact CSS class names from globals.css (.badge, .custom-textarea)
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` — exits 0
- All 7 components exist at their target paths
- `grep -r "export const" frontend/src/components/common/` — shows 7 named exports
- `npm run test:unit` — no regressions
</verification>

<success_criteria>
1. All 7 common components extracted as TypeScript files with correct prop types
2. No `any` types in any component
3. CSS class names preserved exactly (badge, custom-textarea, modal-overlay, fade-in)
4. Build clean — no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-vite-migration/09-03-SUMMARY.md`
</output>
