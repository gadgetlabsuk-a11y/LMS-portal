---
phase: 15-ai-generation-infrastructure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/tests/test_ai_phase15.py
  - frontend/src/hooks/__tests__/useSSEStream.test.ts
  - frontend/src/components/ai/__tests__/SideDrawer.test.tsx
  - frontend/src/components/ai/__tests__/AISuggestionsRail.test.tsx
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05, AI-06, AI-07]

must_haves:
  truths:
    - "Backend test file exists and all stubs emit FAILED (not ERROR) under pytest"
    - "Frontend test files fail at import resolution (vitest collection error: Cannot find module)"
    - "pymupdf==1.26.0 is present in backend/requirements.txt"
  artifacts:
    - path: "backend/tests/test_ai_phase15.py"
      provides: "Wave 0 stubs for AI-03, AI-04, AI-05"
      contains: "pytest.fail()"
    - path: "frontend/src/hooks/__tests__/useSSEStream.test.ts"
      provides: "Failing stub for AI-01"
    - path: "frontend/src/components/ai/__tests__/SideDrawer.test.tsx"
      provides: "Failing stub for AI-02"
    - path: "frontend/src/components/ai/__tests__/AISuggestionsRail.test.tsx"
      provides: "Failing stub for AI-06"
  key_links:
    - from: "backend/tests/test_ai_phase15.py"
      to: "backend/services/document_service.py"
      via: "import DocumentService"
      pattern: "DocumentService"
---

<objective>
Create Wave 0 failing stubs that enforce TDD RED state before any Phase 15 implementation begins. Install PyMuPDF dependency.

Purpose: Establish the test contracts for all AI-01 through AI-07 requirements. Every subsequent plan turns these stubs GREEN — no implementation happens without a prior failing test.

Output: 5 files created (4 test stubs + requirements.txt updated). All must emit failures, not errors.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ai-generation-infrastructure/15-RESEARCH.md
@.planning/phases/15-ai-generation-infrastructure/15-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install PyMuPDF + backend test stubs</name>
  <files>backend/requirements.txt, backend/tests/test_ai_phase15.py</files>
  <action>
Add `pymupdf==1.26.0` to backend/requirements.txt (after python-docx line, grouped with file-processing deps). Then run `cd backend && source venv/bin/activate && pip install pymupdf==1.26.0` to install it in the venv.

Create `backend/tests/test_ai_phase15.py` with the following stub test functions — each must emit `FAILED` (not `ERROR`) when run:

```python
import pytest

@pytest.fixture(autouse=True)
def reset_sse_state():
    from sse_starlette.sse import AppStatus
    AppStatus.should_exit_event = None
    yield


def test_pdf_extraction_pymupdf():
    """AI-04: PyMuPDF extracts text from PDF bytes (not raw UTF-8 decode)."""
    pytest.fail("Not implemented — AI-04 stub")


def test_docx_extraction_bytesio():
    """AI-04: DOCX extraction uses io.BytesIO(file_bytes), not raw bytes."""
    pytest.fail("Not implemented — AI-04 stub")


def test_outline_from_document():
    """AI-03: POST /api/slides/{id}/ai/generate-outline accepts document_url in request body."""
    pytest.fail("Not implemented — AI-03 stub")


def test_sse_stops_on_disconnect():
    """AI-05: SSE generator yields no tokens after is_disconnected returns True (already implemented — verify)."""
    pytest.fail("Not implemented — AI-05 verify stub")


def test_tone_preset_in_prompt():
    """AI-07: ai_tone_preset from course is included in SSE generation prompt."""
    pytest.fail("Not implemented — AI-07 stub")
```

Run `cd backend && source venv/bin/activate && python -m pytest tests/test_ai_phase15.py -v` to confirm 5 × FAILED (not ERROR). The `reset_sse_state` autouse fixture prevents anyio cross-loop RuntimeError in later SSE tests (STATE.md decision from 12-02/13-02).
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend" && source venv/bin/activate && python -m pytest tests/test_ai_phase15.py -v 2>&1 | grep -E "FAILED|PASSED|ERROR|passed|failed|error"</automated>
  </verify>
  <done>5 tests show FAILED (0 errors, 0 passed). pymupdf==1.26.0 in requirements.txt. `import fitz` succeeds in venv.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend test stubs (RED imports)</name>
  <files>
    frontend/src/hooks/__tests__/useSSEStream.test.ts,
    frontend/src/components/ai/__tests__/SideDrawer.test.tsx,
    frontend/src/components/ai/__tests__/AISuggestionsRail.test.tsx
  </files>
  <action>
Create the test directories first if they don't exist:
- `frontend/src/hooks/__tests__/`
- `frontend/src/components/ai/__tests__/`

Create `frontend/src/hooks/__tests__/useSSEStream.test.ts`:
```typescript
import { useSSEStream } from '../useSSEStream'

// AI-01: useSSEStream hook encapsulates fetch+ReadableStream+AbortController
// This import will fail at vitest collection until useSSEStream.ts is created (RED state)
describe('useSSEStream', () => {
  it('returns startStream, cancel, text, isStreaming', () => {
    expect(useSSEStream).toBeDefined()
  })
})
```

Create `frontend/src/components/ai/__tests__/SideDrawer.test.tsx`:
```typescript
import { SideDrawer } from '../SideDrawer'

// AI-02: SideDrawer reusable drawer component
// This import will fail at vitest collection until SideDrawer.tsx is created (RED state)
describe('SideDrawer', () => {
  it('renders when open=true', () => {
    expect(SideDrawer).toBeDefined()
  })
})
```

Create `frontend/src/components/ai/__tests__/AISuggestionsRail.test.tsx`:
```typescript
import { AISuggestionsRail } from '../AISuggestionsRail'

// AI-06: AISuggestionsRail shows completeness nudges
// This import will fail at vitest collection until AISuggestionsRail.tsx is created (RED state)
describe('AISuggestionsRail', () => {
  it('renders nudge cards for modules with missing descriptions', () => {
    expect(AISuggestionsRail).toBeDefined()
  })
})
```

Note: The files `../useSSEStream`, `../SideDrawer`, and `../AISuggestionsRail` do NOT exist yet. Vitest will fail at collection with "Cannot find module" — this is the correct RED state, consistent with Phase 13 and 14 Wave 0 patterns (STATE.md decision from 13-01, 14-01).

Run vitest to confirm collection failures:
`cd frontend && npm run test -- --run src/hooks/__tests__/useSSEStream.test.ts 2>&1 | grep -E "Cannot find|FAIL|Error"`
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/frontend" && npm run test -- --run src/hooks/__tests__/useSSEStream.test.ts 2>&1 | grep -E "Cannot find|FAIL|failed" | head -5</automated>
  </verify>
  <done>3 frontend test files exist. Each causes vitest collection failure with "Cannot find module" for its non-existent target. `ls frontend/src/hooks/__tests__/useSSEStream.test.ts` returns 0.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ai_phase15.py -v` → 5 × FAILED, 0 errors
- `python -c "import fitz; print(fitz.__version__)"` in venv → prints version (not ModuleNotFoundError)
- `grep pymupdf backend/requirements.txt` → matches `pymupdf==1.26.0`
- `npm run test -- --run src/hooks/__tests__/useSSEStream.test.ts` → Cannot find module error
- `npm run test -- --run src/components/ai/__tests__/SideDrawer.test.tsx` → Cannot find module error
- `npm run test -- --run src/components/ai/__tests__/AISuggestionsRail.test.tsx` → Cannot find module error
</verification>

<success_criteria>
All 5 stub tests fail in the correct way (FAILED not ERROR for backend; Cannot find module for frontend). PyMuPDF installed. No implementation exists yet.
</success_criteria>

<output>
After completion, create `.planning/phases/15-ai-generation-infrastructure/15-01-SUMMARY.md`
</output>
