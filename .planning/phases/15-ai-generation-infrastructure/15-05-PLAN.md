---
phase: 15-ai-generation-infrastructure
plan: 05
type: execute
wave: 3
depends_on: [15-02, 15-03, 15-04]
files_modified: []
autonomous: false
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05, AI-06, AI-07]

must_haves:
  truths:
    - "All AI generation surfaces use the shared SideDrawer + StreamingTextOutput components"
    - "PDF upload in Slide Outline Wizard returns a structured outline from document content"
    - "AI suggestions rail shows completeness nudges in Course Builder"
    - "Setting tone to 'casual' in Modal 1A produces visibly different phrasing than 'formal'"
    - "Closing the browser tab mid-stream stops server-side token generation"
  artifacts:
    - path: "frontend/src/components/ai/SideDrawer.tsx"
      provides: "Visible in all 4 AI generation surfaces in the browser"
    - path: "frontend/src/components/ai/AISuggestionsRail.tsx"
      provides: "Visible in Course Builder right panel"
  key_links:
    - from: "browser tab close"
      to: "backend SSE generator"
      via: "request.is_disconnected() returns True → generator breaks"
      pattern: "is_disconnected"
---

<objective>
Human verification of all Phase 15 success criteria in the browser.

Purpose: Automated tests verify logic. This plan verifies the user-visible experience end-to-end: consistent drawer UI, PDF ingestion producing real output, tone differences in generation, and disconnect stopping server work.

Output: All 6 checks approved. Phase 15 marked complete.
</objective>

<execution_context>
@/Users/stuartroberts/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stuartroberts/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite before human verify</name>
  <files></files>
  <action>
Run the complete frontend and backend test suites to confirm all tests pass before handing off to human verification:

```bash
cd frontend && npm run test -- --run
cd ../backend && source venv/bin/activate && python -m pytest tests/ -x -q
```

Fix any failures before proceeding to the checkpoint. Both suites must be green.
  </action>
  <verify>
    <automated>cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/frontend" && npm run test -- --run 2>&1 | tail -3 && cd "/Users/stuartroberts/Library/Mobile Documents/com~apple~CloudDocs/CoWork/LMS Platform/backend" && source venv/bin/activate && python -m pytest tests/ -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>Frontend suite: 0 failures. Backend suite: 0 failures including all 5 test_ai_phase15 tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — 6 browser checks (AI-01 through AI-07)</name>
  <action>Human verifies all 6 checks described below in the browser with live dev servers running.</action>
  <what-built>
    Phase 15 complete implementation:
    - useSSEStream hook + SideDrawer + StreamingTextOutput shared components
    - All 4 AI generation surfaces refactored to use shared components
    - PyMuPDF PDF extraction + DOCX BytesIO fix in document_service.py
    - document_url field on AiOutlineRequest for document-driven outline generation
    - AISuggestionsRail in CourseBuilderPage right panel
    - Course ai_tone_preset propagated to ModuleDetailPage and NarrationTab
  </what-built>
  <how-to-verify>
Start the dev servers: `cd backend && source venv/bin/activate && uvicorn main:app --reload` and `cd frontend && npm run dev`.

**Check 1 — Shared drawer on all surfaces (AI-01, AI-02)**
Open each AI generation surface and confirm the consistent sliding right panel appears:
- Course Identity Modal (Modal 1A) → click "Generate with AI" on description → drawer slides in from right with StreamingTextOutput
- Module Detail page → click "Generate with AI" → same drawer UI
- Slide Editor → Narration tab → click "Generate narration" → same drawer UI
- Slide Builder → "AI Outline" button → SlideOutlineWizard opens (this is a multi-step wizard, not the drawer — confirm it still works after refactor)
Approved if: All 4 surfaces show consistent streaming UI. No raw inline streaming in any surface.

**Check 2 — PDF upload in Slide Outline Wizard (AI-03, AI-04)**
In Slide Outline Wizard Step 1, upload a real PDF file (any PDF with readable text). Complete the wizard steps. Confirm the generated slide titles/content reflect the actual document content, not generic placeholder text.
Approved if: Slide outline is clearly derived from the uploaded document's content.

**Check 3 — DOCX upload (AI-04)**
Repeat Check 2 with a DOCX file.
Approved if: Slide outline reflects DOCX content.

**Check 4 — AI suggestions rail (AI-06)**
Open Course Builder for a course that has at least one module with no description and one module with no videos. Confirm the right rail shows nudge cards for both gaps.
Approved if: Nudge cards visible, correct titles, "Generate" button present on each card.

**Check 5 — Tone preset propagation (AI-07)**
In Modal 1A, set ai_tone_preset to "casual". Generate a module description in Module Detail. Then set tone to "formal" and generate again. Compare the two outputs.
Approved if: Wording differs meaningfully (casual: informal language; formal: professional language).

**Check 6 — Disconnect stops SSE (AI-05)**
Start a long generation (e.g. slide outline from a large document). While tokens are streaming in, close the browser tab or click "Stop" to abort the request. Check backend logs for evidence the SSE generator detected disconnection and stopped.
Approved if: Backend log shows "is_disconnected" path triggered OR generation clearly stops token emission after abort.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual verification only</automated>
  </verify>
  <done>All 6 checks approved by human verifier. Phase 15 complete.</done>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which checks failed and what was observed.</resume-signal>
</task>

</tasks>

<verification>
Human verifier confirms all 6 browser checks pass.
</verification>

<success_criteria>
All 6 checks approved. AI-01 through AI-07 verified end-to-end in browser. Phase 15 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/15-ai-generation-infrastructure/15-05-SUMMARY.md`
</output>
