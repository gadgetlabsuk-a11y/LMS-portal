---
plan: 15-06
phase: 15
title: "AI-02 Gap Closure — Wire SideDrawer into all generation surfaces"
gap_closure: true
closes_gap: AI-02
wave: 6
---

# Plan 15-06 — AI-02 Gap Closure: Wire SideDrawer into All Generation Surfaces

**Goal:** `SideDrawer` is imported and rendered by every AI generation surface.

**Surfaces to update:**
1. `SideDrawer.tsx` — raise z-indices so it appears above the `Modal` component (z-50)
2. `NarrationTab.tsx` — wrap generate controls in SideDrawer
3. `ModuleDetailPage.tsx` — move AI panel into SideDrawer
4. `CourseIdentityModal.tsx` — wire SideDrawer for description + objectives generation
5. `SlideOutlineWizard.tsx` — document architectural exception (wizard is the surface)

---

## Task 15-06-01: Raise SideDrawer z-indices + add drawerClassName prop

**File:** `frontend/src/components/ai/SideDrawer.tsx`

Change overlay from `z-40` to `z-[55]` and panel from `z-50` to `z-[60]` so SideDrawer
renders above the Modal component (z-50) when triggered from within a modal.

---

## Task 15-06-02: Wire SideDrawer into NarrationTab

**File:** `frontend/src/components/slide/NarrationTab.tsx`

- Add `drawerOpen` state (default `false`)
- "Generate" button → `setDrawerOpen(true)` and calls `handleGenerate()`
- SideDrawer wraps StreamingTextOutput (preview of streaming narration)
- Drawer title: "Generate Narration"
- `data-testid="narration-ai-drawer"` on SideDrawer
- Closing the drawer does not clear the narration (it's already in the store)

---

## Task 15-06-03: Wire SideDrawer into ModuleDetailPage

**File:** `frontend/src/pages/creator/ModuleDetailPage.tsx`

- Add `aiDrawerOpen` state (default `false`)
- Replace inline "AI generation panel" div with:
  - A "Generate with AI" trigger button (opens drawer)
  - `<SideDrawer>` containing the prompt textarea, file upload, generate button, preview
- `data-testid="module-ai-drawer"` on SideDrawer
- Streaming still writes to `description` state (no change to streaming logic)

---

## Task 15-06-04: Wire SideDrawer into CourseIdentityModal

**File:** `frontend/src/components/course/CourseIdentityModal.tsx`

- Add `descDrawerOpen` state and `objDrawerOpen` state
- Add `generatedDesc` and `generatedObj` state for drawer previews (separate from form fields)
- Description "Generate with AI" button → opens `descDrawerOpen` SideDrawer
  - SideDrawer title: "Generate Description"
  - StreamingTextOutput shows `generatedDesc`
  - "Apply" button sets `description(generatedDesc)` and closes drawer
  - `data-testid="description-ai-drawer"` on SideDrawer
- Objectives "Generate with AI" button → opens `objDrawerOpen` SideDrawer
  - SideDrawer title: "Generate Objectives"
  - StreamingTextOutput shows raw streaming text
  - "Apply" button parses lines → setObjectives() and closes drawer
  - `data-testid="objectives-ai-drawer"` on SideDrawer

---

## Task 15-06-05: Document SlideOutlineWizard exception

**File:** `frontend/src/components/slide/SlideOutlineWizard.tsx`

Add a comment at top of file:
```
// AI-02 architectural exception: SlideOutlineWizard IS the generation surface —
// a multi-step wizard overlay. It does not use SideDrawer because the wizard
// itself provides the structured UI for document upload, prompt editing,
// step navigation, and streamed JSON output parsing.
```

Also update `.planning/REQUIREMENTS.md` AI-02 note.
